# STATE.md

## Current State

- `/role-model-router/packages/adapter-execution/`, `/role-model-router/packages/provider-openai/`, `/role-model-router/apps/runtime-host-bridge/`, and `/role-model-router/apps/runtime-host-bridge/src/benchmark-runner.ts` now preserve cross-provider tool-call parity across Codex Subscription Responses and chat-completions-compatible DeepSeek or Kimi or LiteLLM paths: caller-owned `parallel_tool_calls` survives end to end, forced Codex tool choice is translated into the official Responses named-tool shape, official typed `function_call` and `function_call_output` continuation items are accepted at ingress, native Codex non-stream outputs keep truthful tool-call structure, Codex tool-bearing benchmark turns use Responses, and rebuilt-runtime Pi proof now succeeds for exact `chatgpt/gpt-5.4` plus alias `difficulty.remote-only`, which selected DeepSeek Pro for the retained alias receipt.
- `/role-model-router/apps/runtime-ui/app/lib/runtime-api.ts`, `/role-model-router/apps/runtime-ui/app/lib/view-models.ts`, the remediated `P0` runtime-ui routes, and `/role-model-router/apps/runtime-host-bridge/{src/cli.ts,scripts/start.ts,scripts/prod-launcher.ts,src/validate-packaging.ts}` now harden route startup and rebuilt-runtime parity together: `/app/models` boots from accounts/endpoints/models/controller/role-policy/router-candidates and defers request evidence with truthful `null` / `Unavailable` semantics, `/app/router`, `/app/router/controller`, `/app/connect`, `/app/connect/upstream`, `/app/system/peers`, and the Studio startup routes no longer boot through `fetchRuntimeSnapshot()`, non-QA startup paths now expose `/api/role-model/requests/latest-ids`, and packaging validation waits for `/api/role-model/runtime/summary` before downstream control-plane checks while preserving the run-66 providers latest-ids baseline.
- `/role-model-router/apps/runtime-ui/app/routes/providers.tsx`, `/role-model-router/apps/runtime-ui/app/lib/runtime-api.ts`, `/role-model-router/apps/runtime-host-bridge/src/index.ts`, and `/role-model-router/packages/sqlite-memory/src/index.ts` now treat recent-request data on `/app/remote/providers` as a deferred lightweight follow-up instead of a first-render dependency: the route boots from provider/account/model/runtime-readiness surfaces only, then calls `GET /api/role-model/requests/latest-ids?limit=10`; the backend serves that path from an ids-only SQLite query that does not read or parse `runtime_observations.observation_json`; rich `/api/role-model/requests` remains the canonical inspection surface; and rebuilt-runtime QA proved the providers page becomes visible before the deferred fetch resolves and stays loaded when that follow-up fails.
- `/role-model-router/packages/provider-openai/`, `/role-model-router/apps/runtime-host-bridge/`, `/role-model-router/packages/protocol-routing/`, `/role-model-router/packages/runtime-observability/`, `/protocol/`, and `/packages/pi-role-model/` now preserve prompt-cache truth end to end for OpenAI-family execution surfaces: OpenAI-family prompt caching is advertised as implicit support, supported-zero requests serialize `cached_tokens: 0` instead of unsupported, current Kimi top-level `usage.cached_tokens` is normalized, continuity is tracked per upstream cache domain with create-versus-restore diagnostics, rebuilt-runtime Pi proof on `chatgpt/gpt-5.4` and `deepseek/deepseek-v4-flash` matches canonical request receipts, and alias `difficulty.remote-only` restores DeepSeek cache on an `A -> B -> A` return leg while Kimi remains non-routable in the current QA runtime because it has `0` benchmark samples and no eligible target.
- `/packages/pi-role-model/` now honors `ROLE_MODEL_ENDPOINT` for runtime request and inspection commands and preserves downstream `piMapping.compat` prompt-cache plus session-affinity hints when mapping Role-Model runtime discovery into Pi provider config and active alias selection.
- `/.agents/skills/recursive-mode/` and the repo-owned `/.recursive/` scaffold now match the current upstream recursive-mode bootstrap contract without letting the bootstrap overwrite repo-owned history: the project skill package again includes its scripts, references, and subskills; `/.recursive/RECURSIVE.md` now reflects `recursive-mode-audit-v2`; `/.recursive/config/recursive-router.json`, `/.recursive/scripts/recursive-training-*`, and `/.recursive/memory/training/` are now present; and `STATE.md`, `DECISIONS.md`, plus the durable memory router remain manually curated.
- `/.github/workflows/docs-site-deploy.yml` now treats missing Cloudflare deploy secrets as a non-fatal skip on non-PR events: docs still build, the static artifact is uploaded, and the workflow emits a clear skip notice instead of failing `main` for an environment-only deploy precondition.
- `/.github/workflows/build-binaries.yml` now keeps release-archive attestation mandatory but retries transient Rekor/Sigstore failures up to three times with short backoff before failing a matrix job, preventing one-off transparency-log timeouts from invalidating an otherwise healthy release build.
- `/role-model-router/packages/adapter-execution/`, `/role-model-router/packages/provider-openai/`, `/role-model-router/packages/runtime-observability/`, `/role-model-router/packages/sqlite-memory/`, `/role-model-router/apps/runtime-host-bridge/`, `/role-model-router/apps/runtime-ui/`, and `/packages/pi-role-model/` now preserve provider identity separately from execution-vendor and adapter identity: `providerFamily` follows the actual provider (`openai`, `deepseek`, `llama-swap`), `vendorId` records intermediaries such as `litellm`, `llama-swap`, and `chatgpt-codex-responses`, request detail renders provider or vendor or execution or adapter as separate facts, and both `runtime:validate-vendors` plus the rebuilt-runtime Pi/Craft alias proof on `difficulty.remote-only` validate that split.
- `/role-model-router/apps/runtime-host-bridge/`, `/role-model-router/packages/adapter-execution/`, `/role-model-router/packages/provider-openai/`, and the shared LiteLLM-backed remote path now preserve richer OpenAI Responses semantics through routed execution: `tool_choice`, reasoning/thinking controls, `previous_response_id`, prompt-cache hints, and session-affinity hints now survive into the shared execution contract, forced tool choice still drops after tool-output continuation turns, and Codex Subscription execution uses the native ChatGPT Codex Responses transport with `providerId = openai`, `vendorId = chatgpt-codex-responses`, and `adapterFamily = codex-subscription-responses` without adding a provider-specific first-attempt routing preference.
- Routed streaming is handled as an execution concern rather than a routing eligibility gate: upstream reasoning deltas are forwarded as OpenAI-compatible `reasoning_content` when present, GPT/Codex Subscription rows record upstream reasoning absence instead of fabricating thinking text, and role-aware Chat Completions to Responses conversion maps replayed assistant history to `output_text` or `refusal` while user input remains `input_text` or `input_image`.
- `/role-model-router/packages/vendor-litellm/` and `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now preserve additive upstream LiteLLM `router_settings` and `litellm_settings` blocks from unified runtime config into managed vendor startup instead of reducing the generated config to `model_list` only.
- `/role-model-router/packages/runtime-observability/`, `/role-model-router/packages/sqlite-memory/`, `GET /api/role-model/requests`, `GET /api/role-model/requests/:id`, and `GET /api/role-model/telemetry/requests` now persist canonical execution-semantics receipts for `sourceClient`, `executionFamily`, `adapterFamily`, provider request/response payload bytes, retry/reroute counts, cooldown/idempotency decisions, Codex selected-backend parameter sanitization, routed provider failure observations, and tool side-effect state. `runtime:validate-vendors` now also emits a deterministic `200`-case Pi/Craft corpus with stable machine-readable routing and execution receipts.
- Upstream execution failures now drive runtime-owned fallback cooldowns with escalating windows of `10m`, `30m`, `1h`, `5h`, `10h`, and `20h`. Timeout, network, rate-limit, quota-exhausted, provider-auth, and upstream-5xx failures remain fallback-eligible, while invalid requests still fail fast without fallback. Repaired Codex auth clears stale provider-auth cooldowns so recovered subscription endpoints can re-enter routing immediately. Future selected-endpoint provider failures persist the selected endpoint/provider/vendor/adapter context and structured failure observation instead of being overwritten by anonymous `routing.failed.pre-execution` telemetry.
- Session bootstrap now treats peer auto-reload degradation as advisory: `peer reload incomplete` still appears in readiness detail, but `/healthz` and session readiness can summarize as `ready` when all non-advisory stages succeeded.
- `/role-model-router/apps/runtime-ui/` now ships against the Paper Linear review baseline rather than the older Apple-reference runtime theme. The repo-owned design authority lives in `role-model-router/apps/runtime-ui/DESIGN_SYSTEM.md`, which now treats the Paper design-system board and Paper runtime-pages board as the visual source of truth for the shared shell, typography, surface tokens, pill/select grammar, and route composition.
- The shared runtime shell now uses a fixed-shell / scrolling-content model: sidebar and header remain pinned while the page-content frame owns scrolling, the content scrollbar is visually hidden, the sidebar is text-only with no visual divider seam, and the header owns the icon-only light/dark toggle plus route-level secondary nav pills.
- Shared runtime-ui controls now use one tokenized text scale and interaction grammar across the app: select triggers, select option rows, filter pills, semantic pills, grouped role-category rows, and advanced-controls expand rows all align to the shared design-system token classes rather than route-local text sizing or hardcoded colors.
- Shared telemetry/chart primitives remain Recharts-backed and now use the Paper-aligned surface treatment plus tighter plot margins so charts do not waste left-side space. Overview and Observe analytics continue to choose chart types by runtime data semantics rather than by mockup mimicry alone.
- Runtime page parity is now tracked against the authoritative Paper runtime-pages board through the approved run-60 screenshot matrix. All shipped route families — Overview, Studio, Local, Remote, Models, Router, Observe, Connect, and System — were re-audited against Paper and user-approved after rebuilt-runtime browser review, and the temporary review-only preview/mock scaffolds used during QA were removed afterward so the runtime remains live-data-driven.
- Role-Model Taxonomy V1 is now the repo-owned canonical vocabulary for request classification, benchmark attribution, telemetry analytics, and routing intent. It defines versioned groups (`engineering`, `product_design`, `knowledge_research`, `business`, `communication`, `governance_safety`), `28` roles, `280` task types, `46` capabilities, `9` modalities, and `15` tool classes with schema version, taxonomy version, database/storage version, content revision, and classification contract version kept separate. Every role declares exactly one `primaryGroupId` and optional `secondaryGroupIds` for cross-listing (e.g., `security` is primary `engineering` + secondary `governance_safety`; `health` is primary `governance_safety` + secondary `knowledge_research`). All 28 roles carry `classification` fields (`summary`, `positiveSignals`, `negativeSignals`) used by the Pi classifier for group-first role scoring. All 7 entity schemas include `replacement` and `deprecationReason` fields for future deprecation support. The canonical runtime data lives under the router/core taxonomy paths, benchmark and telemetry schemas live under `schemas/role-model/taxonomy/`, richer analytics dimensions are centralized in `packages/protocol-types/src/taxonomy-dimensions.ts`, and docs tables for groups, roles, task types, capabilities, modalities, and tool classes are auto-generated from canonical JSON via `scripts/generate-taxonomy-docs.ts`.
- `/role-model-router/apps/runtime-host-bridge/` exposes taxonomy discovery and progressive-disclosure APIs under `/api/role-model/taxonomy*`, validates and normalizes `role_model.intent` request metadata, uses hard taxonomy fields as eligibility filters, uses advisory fields for scoring/diagnostics, and records taxonomy version metadata in routing decisions. The classification guide endpoint (`/api/role-model/taxonomy/classification-guide`) now generates its content from canonical taxonomy data rather than returning hardcoded rules. Decision detail API returns both `normalizedIntent` (camelCase, internal) and `role_model` (snake_case, proposal wire contract) via a `toProposalWireContract()` adapter. Runtime downstream OpenAI discovery now includes Pi-compatible rich records for aliases and fallback QA records, including contract version, type, capabilities, modalities, limits, routable/declared metadata, `piMapping`, and freshness.
- `/role-model-router/apps/runtime-ui/` integrates taxonomy V1 into the existing operator surfaces rather than adding a separate taxonomy app: Models/roles surfaces are group-aware with 6 group headings (`PRODUCT DESIGN`, `ENGINEERING`, `COMMUNICATION`, `KNOWLEDGE RESEARCH`, `BUSINESS`, `GOVERNANCE SAFETY`), new model role assignment defaults to all roles via `roleAssignmentMode: "all"`, roles can be unchecked through visible controls with an all-roles checkbox supporting indeterminate state (`aria-checked="mixed"`), high-risk roles (`health`, `legal`, `finance`, `security`, `recruiter`) are labeled, and task detail is subordinate drill-down under configured model/role context. Observe Requests and Observe Routing now expose richer taxonomy filters and graphs for group, role, task, variant, capability, modality, and tool class dimensions through the shared telemetry design-system primitives, `/app/models` now renders richer taxonomy telemetry rollups plus benchmark-advisory role/group evidence, and request detail now separates original hints, normalized classification, derived analytics tags, telemetry handling, and cost audit data. The providers page (`/app/remote/providers`) correctly displays effective role assignments for all modes by using `buildModelRoleSelection()` which resolves `all`/`include`/`exclude`/`custom` assignment modes, fixing a bug where `binding.roleIds` was read directly and showed "No roles assigned" for `all`-mode models.
- `/packages/pi-role-model/` provides the repo-owned Pi package for connecting Pi to an already-running Role-Model runtime. The public npm baseline remains `@try-works/pi-role-model@0.1.0`, while the current repo package is `@try-works/pi-role-model@0.1.1` and now includes compact taxonomy data, progressive group-first role/task classification, runtime taxonomy discovery with package snapshot fallback, `role_model.intent` injection into real Pi provider requests for known Role-Model aliases, and runtime-owned request/decision inspection commands. The classifier (`classify-with-progressive-disclosure.ts`) uses a two-tier approach: group keyword sets for `selectCandidateGroupIds()`, then `classifyByGroupAndRoleScoring()` scores all candidate roles using their `classification` fields (`positiveSignals`, `negativeSignals`, `summary` from canonical data) and selects the best task from the role's compact chunk via `selectTask()`. Classification context signals — tool names (mapped to role families via a 50+ entry `toolName→roleHints` table), file extensions (mapped via a 20+ entry `fileExtension→roleHints` table), image presence, and file presence — are wired through `ClassificationContext` into group/role scoring. The extension refreshes the effective taxonomy on startup, `/role-model setup`, and `/role-model alias refresh`; `runtime-inspection.ts` powers `/role-model requests` and `/role-model explain latest`; and runtime inspection honors `ROLE_MODEL_ENDPOINT` when no explicit endpoint override is supplied. The package emits `role_model.intent` metadata in the proposal snake_case wire contract with `role_hint_id`, `task_type`, `preferred_capabilities`, `modalities`, `tool_classes`, `taxonomy_version`, `classification_contract_version`, `confidence`, `evidence`, and `alternatives`. It ships a Pi manifest with `pi-package` metadata, `extensions/role-model.ts`, `skills/role-model/SKILL.md`, a single `/role-model` command dispatcher, typed external runtime discovery against `http://127.0.0.1:3456` by default, downstream OpenAI discovery parsing from `/api/role-model/downstream/openai`, compact `/v1/models` fallback, local-first endpoint trust checks, fail-closed auth-required handling, conservative Pi provider metadata mapping, `role-model` provider registration, Pi `setModel` alias selection, file-backed selected-alias state at `~/.pi/agent/role-model.json`, package-local tests, and install/setup docs. The package intentionally does not start the runtime, call the browser-opening launcher, copy credentials, run benchmarks, make hidden classification model calls by default, or read Pi auth files.
- The repository is a pnpm workspace with shared TypeScript and Rust baselines, canonical JSON Schemas under `/protocol/schemas/`, shared root packages under `/packages/`, router packages/apps under `/role-model-router/`, fixtures under `/protocol/fixtures/` and `/testdata/`, and CI under `/.github/workflows/ci.yml`.
- Schema validation and protocol type generation are handled by `/packages/schema-tools/` and `/packages/protocol-types/`; the canonical validation path now validates `19` schema files plus `28` fixture files across valid, invalid, minimal, and edge coverage, and the committed canonical schema sources declare stable in-file `$id` values that both schema-tools and conformance fail fast on if missing or mismatched.
- The deterministic routing core lives in `/role-model-router/packages/core/` and now applies role/task/binding-aware eligibility, provider and endpoint policy filters, canonical strategy aliases, explicit exclusion codes, normalized weighted scoring, full per-candidate metric breakdowns, deterministic tie-break metadata, and stable `app_id`/`org_id` emission in router decisions. The `getQualityMetric` function now gives benchmark-backed quality first-class precedence when benchmark capability data exists: task score → eligible role score → eligible group score → fallback overall benchmark score → measured `judge_score` → measured `quality_score` → default 0.500. `selection_reasons` now include the corresponding benchmark reason codes, measured latency/throughput/reliability still contribute to non-quality metrics, and `MetricSource` includes `"benchmark"` for provenance tracking. `EndpointCandidate` carries assignment-aware `benchmarkCapability` data populated from completed benchmark runs, so benchmark results now influence live routing decisions instead of only operator diagnostics.
- Fixture-driven routing conformance now lives in `/packages/conformance/src/router-fixture-conformance.test.ts` and is backed by the router golden corpus under `/protocol/fixtures/router-golden/cases/`, while `/packages/conformance/src/protocol-fixture-conformance.test.ts` and `/packages/schema-tools/` enforce the expanded schema fixture manifest.
- The observed-performance aggregation path in `/role-model-router/packages/profile-aggregator/` now uses deterministic multi-sample semantics with `measurement_window`, `endpoint_version`, benchmark/live-request source counts, failure/error-class rates, freshness/confidence formulas, and explicit runtime validation of source-count consistency.
- `/role-model-router/packages/trace/` and `/role-model-router/packages/usage/` now provide read helpers plus linkage/summarization helpers, so trace spans, trace events, usage events, and router decisions can be validated as one linked observability model.
- The smoke path in `/role-model-router/apps/gateway-smoke/` now loads a fixture-driven router case, executes the routed adapter-validation path, emits linked router-decision, request-capture, response-capture, normalized-response, adapter-diagnostics, trace, usage, and observed-performance artifacts under `/runtime-output/`, validates them against canonical schemas and linkage helpers before exit, and fails on invalid output.
- The stable config export under `/role-model-router/apps/router-devtools/` emits a normalized ACP/MCP/CLI endpoint inventory with endpoint identity fields plus declared capability metadata in `runtime-output/router-devtools/config-export.json`.
- `/role-model-router/packages/catalog/` now provides the repo-owned models.dev metadata pipeline with explicit root `catalog:refresh` and `catalog:export` entrypoints, pinned snapshot loading, supplement/override layering, inheritance-aware normalization, provider-kind/auth-family enrichment, explicit supported-auth-mode hints, LiteLLM-alignment helpers, and vendor-version ledger derivation.
- `/testdata/catalog/` now holds the pinned refreshed models.dev snapshot plus local supplement fixtures, while `/role-model-router/packages/catalog/data/` holds the durable tracked normalized-catalog and vendor-version-ledger artifacts that now ship as the app's default metadata layer.
- `/role-model-router/packages/provider-account/` now provides explicit runtime-owned provider-account modeling with credential-reference parsing, auth-mode/status/health vocabularies, explicit provider-supported-auth-mode compatibility, region/model policy fields, and validation against the tracked normalized catalog metadata.
- `/role-model-router/packages/sqlite-memory/` now provides the SQLite-first runtime-state location contract, WAL initialization, explicit runtime-memory schema, migration receipts, maintenance defaults, and provider-account persistence for same-host local-disk runtime state.
- `/role-model-router/packages/endpoint-registry/` now provides the runtime-owned endpoint registry with deterministic endpoint construction, account/model/region diagnostics, lifecycle summary output, and the repo-local `runtime:validate-registry` command.
- `/role-model-router/packages/context-envelope/` and `/role-model-router/packages/retrieval-receipt/` now provide bounded continuity-envelope assembly plus deterministic retrieval receipts over the existing SQLite runtime-state baseline.
- `/role-model-router/packages/protocol-routing/` now provides the runtime-owned projection and orchestration layer that composes endpoint-registry, context-envelope, retrieval-receipt, observed profiles, advisory routing-model inputs, and deterministic router-core scoring into one routed decision plus runtime diagnostics.
- `/role-model-router/packages/adapter-execution/` now provides the shared routed execution plane with execution-target resolution, provider-family adapter selection, capture-aware normalized execution results, trace generation, usage-event generation, and the repo-local `runtime:validate-adapter` command.
- `/role-model-router/packages/provider-openai/` and `/role-model-router/packages/provider-anthropic/` now provide the first concrete provider-family request-builder and response-normalizer implementations used by the shared adapter layer.
- `/role-model-router/apps/runtime-host-bridge/` now provides the managed bridge app that exposes `/healthz`, `/v1/models`, and `/v1/chat/completions` over the existing routed adapter-execution path and is exercised by the repo-local `runtime:validate-host` command.
- `/role-model-router/packages/runtime-observability/` now provides the shared runtime-owned observation bundle, grouped diagnostics, capture-policy receipts, persisted profile-feedback shaping, and deterministic OpenTelemetry GenAI export mapping used by both the live host path and smoke.
- `/role-model-router/apps/runtime-host-bridge/` and the vendored host now also expose structured `/api/role-model/requests`, `/api/role-model/requests/:id`, and `/api/role-model/endpoints/:endpointId/profile` inspection reads beside the preserved raw `/logs`, `/logs/stream`, `/api/events`, `/api/metrics`, and `/api/captures/:id` surfaces.
- `/role-model-router/packages/tool-registry/` now provides the runtime-owned provider-agnostic tool registry with strict required-field validation, execution receipts, failed-execution diagnostics, and deterministic connector execution contracts.
- `/role-model-router/packages/provider-mcp/` now extends the prior discovery/export-only MCP surface with runtime connector-definition shaping for MCP-backed tool sources.
- `/role-model-router/apps/runtime-host-bridge/` now surfaces OpenAI-compatible assistant `tool_calls`, executes normalized tool calls through the runtime tool registry, persists tooling receipts in request observations, and exposes the repo-local `runtime:validate-tools` validator.
- `/role-model-router/apps/runtime-host-bridge/`, `/role-model-router/apps/runtime-ui/`, `/role-model-router/packages/sqlite-memory/`, and the packaged runtime path now also preserve truthful provider-account readiness semantics: pending OAuth device flows survive reload/restart, persisted OAuth-backed accounts rehydrate from stored tokens, unresolved env-backed credentials remain `credentials-missing`, and operator surfaces distinguish pending, configured, connected-without-endpoint, and execution-ready states instead of implying saved credentials are immediately usable.
- The compiled runtime dependency graph under `/packages/protocol-types/`, `/role-model-router/apps/runtime-host-bridge/`, and the runtime package set now declares `runtime` export conditions that point plain Node at built `dist` entrypoints while preserving the current source-default developer path.
- `/role-model-router/apps/runtime-host-bridge/` now also exposes `/api/role-model/runtime/summary`, `/api/role-model/providers`, `/api/role-model/accounts`, `POST /api/role-model/accounts`, and `/api/role-model/endpoints`, merging runtime-owned provider presets with the catalog so provider/account onboarding can stay role-model-native.
- `/role-model-router/packages/process-supervisor/`, `/role-model-router/packages/vendor-abstraction/`, `/role-model-router/packages/vendor-llama-swap/`, `/role-model-router/packages/vendor-litellm/`, and `/role-model-router/packages/provider-litellm/` now provide the repo-owned unified vendor execution layer for managed child-process lifecycle, local llama-swap execution, LiteLLM-compatible remote execution, shared `cacheStatus` metadata, routed LiteLLM fallback propagation, and additive vendor-runtime compatibility methods.
- `/role-model-router/apps/runtime-host-bridge/` now supports unified runtime YAML config, derived execution modes (`decision_only`, `local_only`, `remote_only`, `hybrid`), vendor-aware `/healthz` reporting, bridge-owned dispatch between local llama-swap and remote LiteLLM-compatible execution, and unified remote adapter-family reporting as `litellm-proxy`.
- `/role-model-router/sea-config.json` plus `/.github/workflows/build-binaries.yml` now provide the first SEA packaging and multi-platform release path, with embedded llama-swap assets and packaged-runtime validation through `runtime:package-sea` and `runtime:validate-packaging`; the packaged validation flow now rebuilds the current host before SEA injection and proves packaged `/healthz`, `/v1/models`, `/v1/chat/completions`, and `/v1/responses` against the current host/runtime baseline.
- `/role-model-router/apps/runtime-ui/` now provides the first repo-owned runtime operator shell with dashboard, providers, accounts, endpoints, workbench, requests, request detail, and runtime summary routes, while preserving adjacent host links to `/logs`, `/api/metrics`, `/api/captures/:id`, and vendored `/ui`.
- `/testdata/router-runtime/provider-presets.json` now provides the runtime-owned provider-variant layer for Moonshot Open Platform and Kimi Code, and the first UI/provider-account slice keeps Kimi device OAuth visible but explicitly backend-limited rather than pretending the token lifecycle is complete.
- `/role-model-router/vendor/llama-swap/` is now vendored as a nested Go module and owns the local host lifecycle, request forwarding, logs, metrics, captures, and related operator surfaces while delegating routing and execution semantics to the role-model bridge.
- `/role-model-router/vendor/llama-swap/proxy/ui_stub/index.html` and the updated `/role-model-router/vendor/llama-swap/proxy/ui_embed.go` now make the vendored host compile and start on clean worktrees without requiring ignored/generated `proxy/ui_dist` assets, while still preferring real upstream-built assets when present.
- `/role-model-router/packages/sqlite-memory/` now also owns explicit runtime-state export, backup, delete, and restore helpers for operator drills over the existing SQLite-first store.
- The root `corepack pnpm run runtime:validate-operations` command now validates the repaired live host path, scope-isolated runtime state, runtime-data maintenance drills, and replay/shadow comparison on top of the existing host/adapter/routing baseline.
- `/docs/operations/01-router-runtime-hardening-playbook.md` now provides the durable repo-owned operator guide for runtime validation order, vendor refresh, deployment/upgrade guidance, and SQLite maintenance drills.
- `/testdata/router-runtime/registry-sources.json` and `/testdata/router-runtime/context-envelope.json` now hold pinned registry/discovery and continuity-budget fixtures for the run-07 validation path.
- `/testdata/router-runtime/routing-request.json`, `/testdata/router-runtime/routing-observed-profiles.json`, `/testdata/router-runtime/routing-role-task.json`, and `/testdata/router-runtime/routing-model-guidance.json` now hold pinned run-08 routing fixtures for deterministic local/cloud runtime-routing validation.
- `/testdata/router-runtime/adapter-request.json`, `/testdata/router-runtime/adapter-routing-request.json`, `/testdata/router-runtime/adapter-role-task.json`, `/testdata/router-runtime/adapter-captures.json`, `/testdata/router-runtime/adapter-openai-response.json`, and `/testdata/router-runtime/adapter-anthropic-response.json` now hold the pinned run-09 adapter execution fixtures for deterministic routed execution validation without live provider calls.
- `/testdata/router-runtime/provider-accounts.json` now holds pinned provider-account fixtures, and the root `corepack pnpm run runtime:validate-state` command validates account configuration plus SQLite initialization/persistence against the tracked catalog handoff.
- The root `corepack pnpm run runtime:validate-routing` command now validates one end-to-end runtime-routing path and emits the router decision, retrieval-receipt summary, context-envelope summary, and routing-model diagnostics.
- The root `corepack pnpm run runtime:validate-adapter` command now validates one end-to-end routed adapter execution path and emits capture-aware execution diagnostics on top of the existing routing baseline.
- The root `corepack pnpm run runtime:validate-observability` command now validates one end-to-end host-integrated observability path and confirms structured request inspection, endpoint-profile readback, capture access, and OpenTelemetry export linkage.
- The protocol docs under `/docs/protocol/` now describe the hardened M1-M3 baseline, including `measurement_window`, role/task/binding routing semantics, trace and usage linkage, benchmark-to-profile aggregation, and the expanded reason-code vocabulary.
- `/docs/architecture/06-router-runtime-architecture-lock.md` now freezes the first router-runtime milestone as a single-host local-machine runtime built on the committed run-03 baseline, with explicit boundaries for provider taxonomy, account ownership, endpoint instantiation, protocol-driven projection, routing-model control, cache policy, OpenTelemetry interoperability, vendor/frontend/operator scope, and deferred MCP/tool work.
- `/docs/architecture/07-router-runtime-routing-strategy-lock.md` now freezes the repo-owned routing-strategy handoff for alias-based local-plus-remote routing, including the mode vocabulary (`basic`, `difficulty`, `intelligent`, `hybrid`), config ownership, easy/medium/hard rubric, compatibility policy, run mapping for `23` through `30`, and the TDD plus end-to-end verification discipline for later implementation runs.
- `/role-model-router/apps/runtime-host-bridge/` now reads latest observed profiles from SQLite runtime state on each request instead of routing from the startup fixture-only `routing-observed-profiles.json` map.
- `/role-model-router/packages/runtime-observability/` plus the bridge request-observation path now expose `routingDiagnostics.observedProfile` with source, `per-request` read mode, and measured-at metadata so operators can confirm which persisted profile influenced a route.
- The repo-owned validation floor now proves the runtime-owned feedback loop through focused bridge tests plus `runtime:validate-host` and `runtime:validate-vendors`, including local-and-remote readback visibility.
- `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now owns an explicit `observedData` contract with defaults and validation for `metric_decay_percent_per_day` on `latency` and `throughput` plus throughput-SLA policy across local and remote endpoints. Legacy halflife keys can still parse for compatibility, but canonical config truth now renders only the narrowed latency/throughput decay contract.
- `/role-model-router/packages/core/`, `/role-model-router/packages/protocol-routing/`, and the bridge now use observed data so only latency and throughput time-decay during route scoring, on a 10%-per-day retained-deviation loss curve. Benchmark or measured quality, measured reliability, and measured cost remain age-invariant, while SQLite-backed throughput penalties still apply independently.
- `/role-model-router/packages/runtime-observability/` plus the bridge request-observation path now expose `routingDiagnostics.effectiveMetrics` and `routingDiagnostics.throughputPenalty`, including whether a metric was time-decayed or passed through unchanged.
- `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now also owns a `model_aliases` contract that normalizes to `modelAliases` and round-trips through the runtime config surface.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` now exposes alias ids beside real model ids in `/v1/models` and downstream OpenAI-compatible provider guidance, and expands alias requests into pooled real endpoint candidates before existing routing.
- `/role-model-router/packages/runtime-observability/` plus the bridge request-observation path now also expose durable `routingDiagnostics.aliasResolution` metadata with the requested alias id, resolved real model ids, and pooled endpoint ids.
- The repo-owned validation floor now proves one hybrid local-plus-remote alias pool end to end through `runtime-host-bridge` tests, `runtime:validate-vendors`, and agent-operated runtime readback.
- `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now also owns the difficulty-routing contract, including the shared easy-medium-hard rubric family, `difficulty_classifier`, alias mode `difficulty`, and per-source `maxDifficulty`.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` now executes configured difficulty classification with deterministic fallback, maps difficulty to live routing strategy behavior before final selection, and applies mixed local-and-remote `maxDifficulty` gating on difficulty-mode alias requests.
- `/role-model-router/packages/runtime-observability/` plus the bridge request-observation path now also expose durable `routingDiagnostics.difficultyRouting` metadata with assigned difficulty, selected strategy, fallback status or reason, rubric-signal summaries, and excluded endpoint ids.
- The repo-owned validation floor now proves mixed local-plus-remote difficulty routing through `runtime-host-bridge` tests, `runtime:validate-vendors`, and agent-operated readback of easy-path cost routing plus hard-path quality routing and local-endpoint exclusion; medium-path live QA remains automated-evidence-only under the current binary mock classifier.
- `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now also owns an explicit `observed_data.difficulty_learning` contract with conversation-cache invalidation controls plus advisory-recommendation and observed-override thresholds.
- `/role-model-router/packages/sqlite-memory/` and `/role-model-router/apps/runtime-host-bridge/` now persist conversation difficulty cache entries, segmented easy/medium/hard observed profiles, advisory `maxDifficulty` recommendation payloads, observed override explanations, and selected-bucket observed-profile diagnostics across both local and remote endpoints.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` now reuses cached conversation difficulty when invalidation rules allow, reclassifies deterministically when the conversation changes materially, and can use observed per-bucket performance to override configured `maxDifficulty` ceilings without mutating operator config.
- The repo-owned validation floor now proves bucketed endpoint-profile readback, deterministic cache reuse and invalidation, observed override above configured ceilings, and bucket-selected routing through focused tests, runtime validators, and agent-operated readback of live runtime surfaces.
- `/role-model-router/apps/runtime-host-bridge/src/unified-runtime-config.ts` now also owns an explicit `controller` contract with source-type targeting, model or endpoint selection, and bounded timeout behavior for request-time routing guidance.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` now executes request-time controller inference for intelligent aliases on both chat-completions and responses paths, validates structured controller directives, merges accepted guidance into live `RoutingRequest` fields plus `routingModel.preferredEndpointIds`, and fails closed on invalid controller output.
- Runtime request observations, runtime validators, and agent-operated readback now also expose durable `routingDiagnostics.controllerRouting` metadata with controller-active state, accepted directives, and explicit fallback reasons, and prove mixed local-plus-remote controller steering plus invalid-output fallback without breaking exact-model requests.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` now also accepts per-request routing-mode overrides for `baseline`, `difficulty`, `controller`, and `hybrid` on both chat-completions and responses ingress, rejects invalid override values deterministically, and records whether the effective mode came from a request override or alias default.
- `/role-model-router/apps/runtime-host-bridge/src/index.ts` and `/role-model-router/packages/runtime-observability/src/index.ts` now also persist durable `routingDiagnostics.rewrite` and `routingDiagnostics.hybridArbitration` receipts, including rewrite-applied versus rewrite-skipped outcomes, downstream model ids, difficulty strategy, final strategy, and controller-dominant plan changes.
- The repo-owned validation floor now also proves same-pool override-mode execution, exact-model rewrite-skipped compatibility, explicit invalid-override `400` ingress failure, and mixed local-plus-remote readback of override, rewrite, and hybrid-arbitration diagnostics.
- The repo-owned runtime UI now also exposes a structured `Control > Routing strategy` surface, explicit workbench routing-mode override control, request-ledger routing decision readback, and request-detail routing receipts for routing mode, rewrite, difficulty, controller, hybrid, and rubric signals while preserving raw runtime-config and raw observation bundles as advanced escape hatches.
- `runtime:validate-ui` now also seeds and reads back a deterministic routed request so the operator-facing telemetry list and request-detail APIs prove persisted `routingDecisionId`, effective routing mode, and rewrite-reason visibility for the shipped runtime UI.
- `/docs/architecture/05-memory-model.md` now states that the first runtime milestone is architecturally SQLite-first and same-host/local-disk scoped even though the production runtime memory backend is still deferred.
- The downstream routing-strategy run contracts under `/.recursive/run/23-.../` through `/.recursive/run/30-.../` now consume the repo-owned routing-strategy lock directly instead of only the external proposal path.
- Future browser, edge, and native runtime families are still represented as scaffold-grade package/crate boundaries rather than production-complete implementations.
- The current validated runtime command chain is:
  - `corepack pnpm run schemas:validate`
  - `corepack pnpm run runtime:validate-state`
  - `corepack pnpm run runtime:validate-routing`
  - `corepack pnpm run runtime:validate-adapter`
  - `corepack pnpm run runtime:validate-ui`
  - `corepack pnpm run runtime:validate-host`
  - `corepack pnpm run runtime:validate-vendors`
  - `corepack pnpm run runtime:package-sea`
  - `corepack pnpm run runtime:validate-packaging`
  - `corepack pnpm run runtime:validate-observability`
  - `corepack pnpm run runtime:validate-operations`
  - `corepack pnpm run runtime:validate-tools`
  - `corepack pnpm run smoke`
- The root workspace scripts in `/package.json` now invoke nested workspace commands through `corepack pnpm ...`, which restores the canonical wrapper-path behavior used by the conformance suite and other shell-outs and now also exposes the explicit `catalog:refresh` and `catalog:export` entrypoints at the repo root.
- Operational caveats:
  - the repo now expects `Node >=24 <25`, which matches the current environment and removes the earlier Node-engine mismatch
  - `corepack pnpm exec biome check .` still reports pre-existing Windows formatting drift in tracked baseline files and was intentionally kept out of run `03-protocol-baseline-hardening` scope
  - the selected run-09 worktree now reproduces `corepack pnpm --filter @role-model-router/adapter-execution test` PASS, `corepack pnpm --filter @role-model-router/adapter-execution build` PASS, `corepack pnpm --filter @role-model-router/provider-openai test` PASS, `corepack pnpm --filter @role-model-router/provider-openai build` PASS, `corepack pnpm --filter @role-model-router/provider-anthropic test` PASS, `corepack pnpm --filter @role-model-router/provider-anthropic build` PASS, `corepack pnpm --filter @role-model-router/gateway-smoke build` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run schemas:validate` PASS, `corepack pnpm run build` FAIL, `corepack pnpm run test` FAIL, and `corepack pnpm run smoke` PASS because the broader root `build` / `test` chain still hits the inherited `packages/schema-tools` Biome `No files were processed in the specified paths` failure while formatting generated protocol types
  - the selected run-10 worktree now reproduces `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, focused vendored bridge proxy/process tests PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run schemas:validate` PASS, `corepack pnpm run build` FAIL, `corepack pnpm run test` FAIL, `corepack pnpm run smoke` PASS, and vendored `go test ./...` FAIL because the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure while the vendored full-suite red remains the upstream Windows `sleep` test assumption
  - the selected run-11 worktree now reproduces `corepack pnpm --filter @role-model-router/runtime-observability test` PASS, `corepack pnpm --filter @role-model-router/runtime-observability build` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory test` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory build` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, `corepack pnpm --filter @role-model-router/gateway-smoke build` PASS, `corepack pnpm run runtime:validate-observability` PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run schemas:validate` PASS, `corepack pnpm run smoke` PASS, focused vendored `go test ./proxy -run 'Test.*RoleModel.*'` PASS, `corepack pnpm run build` FAIL, `corepack pnpm run test` FAIL, and vendored `go test ./...` FAIL because the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure while the vendored full-suite red remains the upstream Windows `sleep` test assumption
  - the selected run-12 worktree now reproduces `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory test` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory build` PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-observability` PASS, `corepack pnpm run runtime:validate-operations` PASS, `corepack pnpm run runtime:validate-state` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run schemas:validate` PASS, and `corepack pnpm run smoke` PASS while vendored `go test ./proxy` still fails on the upstream Windows `sleep` assumption and the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure
  - the selected run-13 worktree now reproduces `corepack pnpm --filter @role-model-router/tool-registry test` PASS, `corepack pnpm --filter @role-model-router/tool-registry build` PASS, `corepack pnpm --filter @role-model-router/provider-mcp test` PASS, `corepack pnpm --filter @role-model-router/provider-mcp build` PASS, `corepack pnpm --filter @role-model-router/runtime-observability test` PASS, `corepack pnpm --filter @role-model-router/runtime-observability build` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, `corepack pnpm --filter @role-model-router/adapter-execution test` PASS, `corepack pnpm --filter @role-model-router/adapter-execution build` PASS, `corepack pnpm run runtime:validate-tools` PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-observability` PASS, `corepack pnpm run runtime:validate-operations` PASS, `corepack pnpm run runtime:validate-state` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run schemas:validate` PASS, and `corepack pnpm run smoke` PASS while the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure
  - the selected run-14 worktree now reproduces `corepack pnpm --filter @role-model-router/catalog test` PASS, `corepack pnpm --filter @role-model-router/catalog build` PASS, `corepack pnpm --filter @role-model-router/provider-account test` PASS, `corepack pnpm --filter @role-model-router/provider-account build` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory test` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory build` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, `corepack pnpm --filter @role-model-router/runtime-ui test` PASS, `corepack pnpm --filter @role-model-router/runtime-ui build` PASS, `corepack pnpm --filter @role-model-router/provider-openai test` PASS, `corepack pnpm --filter @role-model-router/provider-openai build` PASS, `corepack pnpm run runtime:validate-ui` PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-observability` PASS, `corepack pnpm run runtime:validate-operations` PASS, `corepack pnpm run schemas:validate` PASS, `corepack pnpm run docs:build` PASS, `corepack pnpm run smoke` PASS, live bridged `/v1/responses` JSON and streaming plus `/v1/chat/completions` streaming E2E PASS, `corepack pnpm run build` FAIL, and `corepack pnpm run test` FAIL because the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure while the repo-owned UI/control-plane path stays green
- the selected run-15 worktree now reproduces `corepack pnpm --filter @role-model-router/provider-litellm build` PASS, `corepack pnpm --filter @role-model-router/provider-litellm test` PASS, `corepack pnpm --filter @role-model-router/vendor-llama-swap build` PASS, `corepack pnpm --filter @role-model-router/vendor-llama-swap test` PASS, `corepack pnpm --filter @role-model-router/vendor-litellm build` PASS, `corepack pnpm --filter @role-model-router/vendor-litellm test` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge build` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge test` PASS, `corepack pnpm run runtime:validate-host` PASS, `corepack pnpm run runtime:validate-vendors` PASS, `corepack pnpm run runtime:validate-packaging` PASS, `corepack pnpm run runtime:validate-adapter` PASS, `corepack pnpm run runtime:validate-routing` PASS, `corepack pnpm run runtime:validate-observability` PASS, `corepack pnpm run runtime:validate-operations` PASS, `corepack pnpm run runtime:validate-tools` PASS, `corepack pnpm run runtime:validate-ui` PASS, and `corepack pnpm run schemas:validate` PASS while the broader root `build` / `test` chain still hits the inherited schema-tools/Biome generated-types failure
- the selected run-16 worktree now reproduces `corepack pnpm --filter @role-model-router/runtime-observability exec vitest run test/index.test.ts` PASS, `corepack pnpm --filter @role-model-router/sqlite-memory exec vitest run test/index.test.ts` PASS, `corepack pnpm --filter @role-model-router/runtime-host-bridge exec vitest run test/index.test.ts test/validate-vendors.test.ts` PASS, `corepack pnpm --filter @role-model-router/runtime-ui test` PASS, `corepack pnpm --filter @role-model-router/runtime-ui build` PASS, `corepack pnpm run runtime:validate-ui` PASS, `corepack pnpm run runtime:validate-vendors` PASS, `corepack pnpm run types:generate` PASS, `corepack pnpm run schemas:validate` PASS, `corepack pnpm --filter @role-model/schema-tools exec vitest run test/validate-schemas.test.ts test/generate-protocol-types.test.ts` PASS, `corepack pnpm --filter @role-model/schema-tools build` PASS, and `corepack pnpm --filter @role-model-router/process-supervisor test` PASS in isolation while the broader root `build` now fails only on the unrelated `provider-acp` / `provider-cli` endpoint-kind mismatch and the broader root `test` run still hits the workspace-level `process-supervisor` crash-callback flake
- the local runtime-state validation path currently uses built-in `node:sqlite`; it works in the selected Node 24 environment and remains intentionally visible as an experimental platform dependency rather than being hidden behind a silent fallback
- the bridge and provider-openai layers now support OpenAI-compatible downstream streaming for routed `/v1/chat/completions` and `/v1/responses`; live bridged host-path E2E is green, while `moonshotai/kimi-k2.5` remains modeled on the current `openai.chat.completions` path rather than `openai.responses`
- the repo-owned runtime UI now ships a hierarchical shell with `Overview`, `Studio`, `Local`, `Remote`, `Models`, `Router`, `Observe`, `Connect`, and `System` sections; controller/models surfaces, activity/log drill-ins, vendor-backed studio workspaces, and upstream/downstream/system pages are now part of the single-host operator baseline
- the runtime UI now uses a **Connect** pillar at `/app/connect`, `/app/connect/downstream`, and `/app/connect/upstream` for application-facing registry and OpenAI-compatible contract guidance, while **Local → Endpoints** at `/app/local/endpoints` remains the separate device-inference peer inventory; legacy `/app/endpoints*`, `/app/control/endpoints`, and `/app/integrations/*` routes redirect to the Connect paths
- the runtime UI shell is quieter after run 35: left-rail page counts and meta-guidance panels are removed, Overview shows a slim latest-requests teaser with a CTA to Observe → Requests, Local Matrix merges into Local Models **List | Grid** at `/app/local/models?view=grid`, Router guidance provenance and policy inputs live on Router Overview with `/app/router/config` redirecting to `/app/router`, and Connect registry hands alias posture off to Router instead of duplicating alias tables
- the runtime UI now also ships a shared `DisclosureSection` primitive that collapses dense request-detail and model-inspection secondary groups by default; frontend manual QA for UI-only runs should use browser-session visual verification with screenshot evidence, not test-encoded contracts alone
- **Run 36 (consumption + telemetry + benchmark):** bridge execution enriches the catalog at request time for peer-backed endpoints; provider-openai and workbench surface `reasoning_content`; `getLocalLogs()` falls back to telemetry when llama-swap is inactive and `/logs/stream` returns JSON 503 before static root; HTTP `latencyMs` is measured; `x-role-model-request-id` aliases `x-request-id`; failed chats persist failure telemetry rows
- packaged-runtime QA (addendum 01) proved R1–R6 on operator `:3456` with live local LFM and remote Kimi k2.6; addendum 02 fixed SP7 sole-candidate throughput SLA hard-deny and SP8 partial runtime-config merge; addendum 03 recorded a 46-prompt × 4-strategy routing matrix for tuning decisions
- consumer difficulty routing on packaged `:3456` with alias `mixed.local-remote` passed 14/15 E2E scenarios (hard→remote 3/3, easy→local 3/3); default throughput SLA can still hard-deny sole exact-remote candidates below 24 tps unless disabled or a local pool alternative exists
- the canonical routing-capability **benchmark operator workflow** lives in `/.recursive/BENCHMARK-WORKFLOW.md` with machine validation via `evidence/scripts/validate-benchmark-run.py`; **Models → Benchmark** at `/app/models/benchmark` exposes summary, preferences, per-model scores, and clear-benchmark-data; addenda 04–10 delivered judge pipeline hardening and operator run `c0b66038` (**VALID**, **HEALTHY** control)
- Run 45 realigned Observe on the packaged operator surface: `/app/observe` lands on the Requests ledger, Requests/request detail are the canonical structured telemetry flow, Activity and Logs are explicit preserved raw-host adjacency pages, and packaged log rows with bracketed timestamps now correlate back into request detail on `:3456`
- Run 47 extended that operator baseline: failed request rows now retain caller correlation plus explicit failure-stage endpoint markers in the canonical telemetry ledger, `/app` starts with `Recent telemetry window` and uses an interaction-level latest-requests rail, `/app/observe/activity` preserves backend newest-first metrics order, `/app/router` alias inventory now separates configured hints, resolved models, and readiness while leaving backend-truth alias-drift warnings visible until config actually changes, and redundant router overview or strategy context panels have been removed so the remaining router surfaces stay focused on canonical inventory and active controls
- Run 48 replaced the runtime-ui's remaining Swiss-authority contract with an Apple-inspired design-system baseline: `DESIGN_APPLE_REFERENCE.md` is the visual reference, `DESIGN_SYSTEM.md` is the repo-owned contract, only explicit `Light` and `Dark` themes ship, the theme toggle now lives in the sidebar, shared typography/control tokens use SF Pro first with Inter fallback, semantic status pills keep transparent backgrounds, shared shell/card/disclosure primitives no longer draw internal divider lines, custom dropdowns use themed select controls, and packaged-runtime QA on `:3457` now depends on the rebuilt runtime-ui asset-sync path plus the stable shell-header contract so browser proof matches the current worktree build
- the first runtime-ui onboarding slice still shows Moonshot Open Platform as ready and Kimi Code as backend-limited with real device-OAuth metadata; account upserts are live, but endpoint rows still remain controlled by the current registry baseline rather than being auto-created by account save
- the runtime now exposes canonical unified telemetry summary, ledger, request-detail, and `/api/role-model/telemetry/stream` SSE surfaces through the bridge, and the repo-owned runtime UI dashboard, requests, and request-detail pages consume that mixed local-plus-remote telemetry baseline directly
- Run 49 extends that telemetry baseline with backend-owned historical telemetry analytics, persisted per-request cost/cache/routing facts, `POST /api/role-model/telemetry/query`, charted `/app` overview and Observe analytics pages, Apple-reference chart/design tokens, and no chart placement on setup/config routes
- Run 49 also repairs the operator shell around the Apple-themed design-system contract: shared typography/control roles, quiet rounded panels, sidebar theme toggle, themed keyboard-searchable listbox/dropdown controls, distinct same-chart series colors, compact fact-card values, and removal of redundant eyebrow/divider/context panels
- Run 49 makes router candidates the canonical source for configured-and-available routing inventory, persists strategy/execution-mode edits through the editable unified runtime config path, derives aliases from the effective strategy/execution mode instead of stale mixed local-remote labels, and gates benchmark runs against execution-mode-ineligible endpoints before execution starts
- Run 50 extends the OpenAI remote-provider baseline: operator surfaces now show one `OpenAI` provider with `API Key` plus `Codex Subscription`, raw `chatgpt` no longer appears as a duplicate provider row, and OpenAI subscription support is curated to the GPT `5.3+` family instead of blindly inheriting raw LiteLLM `chatgpt/*` inventory
- Run 50 also makes `Codex Subscription` truthful at runtime boundaries: device-code start/poll can complete from the local Codex auth cache, connected accounts can persist as `Connected, no endpoint` / `entitlement-missing`, Studio honors that no-endpoint posture, and the runtime explicitly blocks direct OpenAI Platform transport when the cached ChatGPT/Codex session lacks those request scopes
- Run 50 now makes routing posture canonical in the control plane: the runtime persists the full strategy × execution-mode alias matrix, removes legacy `craft-ask` strategy/alias ids, uses the execution-mode-specific matrix as the alias truth surface, widens controller timeout/output-budget compatibility, and repairs non-controller `requestedRoleId` handling before router-core role filtering
- Run 50 also clarifies tool/capability routing semantics across remote providers: exact OpenAI hosted search stays provider-native for supported GPT `5.3+` subscription models, Kimi exact hosted search is treated as provider-native and can converge to final answers, DeepSeek DSML search markup is normalized back into consumer-visible tool calls rather than hosted inside the router runtime, and routing metadata must not exclude Kimi or DeepSeek from tool-eligible requests they actually support
- Run 50 updates the operator and documentation baseline around those routing changes: the Roles UI is role-first with task drill-down, `/docs/architecture/09-runtime-routing-strategy-interactions.md` is the current routing interaction reference, and `runtime:validate-ui` now exits cleanly after shutting down its managed backend
- the repo-owned control plane now includes `GET` and `PUT /api/role-model/runtime/config`, `Control > Runtime Config`, live account save or device-OAuth status, endpoint activation, and honest zero-endpoint controller/models/runtime empty states instead of 500 or loading traps
- the schema-tools and generated-types path now emits titled helper types for internal `$defs` entries and preserves `UsageEvent.cost_actual`, so focused `types:generate`, `schemas:validate`, and `@role-model/schema-tools build` are green in the run-16 slice
- the runtime now owns router-grade role/task policy persistence at `runtimeStateRoot\role-policy.json`; bridge CRUD/readback routes and router inputs now consume the runtime-owned policy instead of the old fixture-fed `adapter-role-task.json` source
- the runtime UI now exposes `Control > Roles` for live role create or edit plus task allowlist editing, and `Control > Models` can now mutate model-side role bindings against the same runtime-owned policy surface
- request ingress now accepts `requestedRoleId` through bridge options and the `x-role-model-requested-role-id` header; the selected role can inject `default_system_instructions`, constrain `tool_policy`, surface `output_contracts` and `safety_policy_refs`, and persist the applied policy in `routingDiagnostics.rolePolicy`
- the QA bridge launcher now forwards the role-policy, model inventory, device-authorization, and editable unified runtime-config path needed for live Roles/Models plus routing-strategy browser proof
- the vendored llama-swap managed bridge must launch `role-model-router/apps/runtime-host-bridge/src/cli-entry.ts`; using `src/cli.ts` exits without serving `/healthz` and breaks `runtime:validate-host`
- Run 16 Addendum 03 (Frontend Config Remediation) committed: removed hardcoded defaults from workbench.tsx, exposed full provider catalog (108 LiteLLM providers), local model CRUD in Providers page
- Run 16 Addendum 04 (LiteLLM-only catalog) committed: cleared all hardcoded providers and models from normalized-catalog.json, cleared local CLI endpoint from registry-sources.json, added runtime model synthesis for fixture-referenced and dynamically activated models
- Run 16 Addendum 05 (Local Model Config UI) committed: full CRUD form for llama-swap models in Providers page with add/edit/delete/save
- Run 16 Addendum 06 (Fixture Loading Parameterization) committed: dual fixture strategy with `fixtureRoot` parameter, runtime UI starts clean (empty fixtures) while tests/validation use preserved fixtures from `testdata/router-runtime/fixtures/`, Go bridge process supports `ROLE_MODEL_BRIDGE_FIXTURE_ROOT` env var
- All validation commands green on main: `runtime:validate-ui`, `runtime:validate-host`, `runtime:validate-vendors`, `schemas:validate`, `smoke`
- Run 17 (OAuth LiteLLM Generalization) merged to main at `8e4bde2`: R1–R8 implemented — generalized device headers for any OAuth provider, added OAuth metadata to LiteLLMProviderInfo, added scope support, renamed credential backend to `local-file`, unified config OAuth detection, in-memory refresh updates, oauthHost field cleanup, frontend OAuth variant display with DESIGN_SYSTEM.md updates
- Post-R17 fixes committed: restored Kimi OAuth chat headers (`cc7d163`), UI error handling in result panel (`e74c273`), validate-ui test fixes for auto-generated variants (`89d3b41`), CORS support for external app connections (`8e451e9`), always-show-llama-swap fix (`3fd76ea`), dual base URL options on downstream page (`8351bd6`)
- Run 16 Addendum 07 (Local Inference Server Providers) committed: replaced synthetic `llama-swap` provider with four actual inference servers — `llamacpp`, `vllm`, `tabbyapi`, `stable-diffusion-cpp` — all with `providerKind: "local-engine"`, keeping llama-swap runtime config underneath
- All validation commands green on main: `runtime:validate-ui`, `runtime:validate-host`, `runtime:validate-vendors`, `schemas:validate`, `smoke`
- All test suites green: runtime-host-bridge (40), provider-account (6), adapter-execution (5), protocol-routing (4), runtime-observability (2), catalog (4), conformance (2), runtime-ui (61)
- Run 18 (Local llama-swap Runtime) merged to main at `98b7d17`: SP1–SP6 completed — new "Local" sidebar section with Models, Swap History, and Policy pages; bridge API endpoints for local runtime state (`/api/role-model/local/models`, `/swap`, `/policy`); full page implementations with Swiss design system; browser verification with screenshots; all validations green
- Run 19 (Local llama-swap Proxy) merged to main at `15597d2` via GitHub PR #5: wired local runtime methods to llama-swap proxy — `getRunningModels`, `unloadModel`, `getLogs` (later removed in Run 20); 40/40 bridge tests, 61/61 UI tests; all validations green
- Run 20 (Local llama-swap Completion) completed in worktree branch `recursive/20-local-llama-swap-completion`:
  - R1: File-backed policy persistence (`local-policy.json`) with defaults (`ttl: 300`, `maxConcurrency: 1`, `autoUnload: true`)
  - R2: SQLite-backed swap history (`llama_swap_events` table) with events wired to load/unload
  - R3: Removed dead `getLogs` from `VendorRuntime` interface and `vendor-llama-swap`
  - R4: Documented `loadedAt` fabrication in `vendor-llama-swap`
  - R5: Updated `DESIGN_SYSTEM.md` with new routes/templates; `ui-design-system` skill audit: 0 blockers
  - R6: Created `/app/local/logs` page with auto-refresh, line count, empty state
  - R8: Created `/app/local/matrix` page with loaded model grid
  - R9: Created `/app/local/peers` page with inventory and add-peer form
  - R10: Browser verification — screenshots captured for all 6 Local pages
  - 46/46 bridge tests (40 existing + 6 new local-policy tests), 61/61 UI tests
  - All validations green: `runtime:validate-host`, `runtime:validate-vendors`, `runtime:validate-ui`, `schemas:validate`, `smoke`
  - Deferred: R7 (Model-level overrides UI), R2.5 (Auto-detected swap events)

- Run 21 (Semantic Color System + Deferred Completion) completed in worktree branch recursive/21-semantic-color-system:
  - R1-R8: Semantic color system overhaul — cobalt blue (#003B8E) primary accent, red (#C8102E) reserved for errors, green (#166534) for success, amber (#b45309) for warning; dark mode with light blue (#60a5fa) accent
  - R9: Model-level overrides — backend persistence to model-overrides.json, frontend controls on /local/models for TTL, context window, concurrency limit per model
  - R10: Auto-detected swap events — background 5s polling of listLocalModels(), automatic SQLite event insertion on model change
  - R11: Peer passthrough backend — readPeers, updatePeers, checkPeerHealth methods, JSON persistence, health proxy endpoint, full frontend integration on /local/peers
  - 53/53 bridge tests (49 existing + 4 new peer tests), 61/61 UI tests
  - Schema validation passes
  - Browser screenshot verification blocked by display surface unavailability; verified via unit tests and build validation instead

- Run 32 (Router Runtime Routing Operator Surface) completed in worktree branch `recursive/32-router-runtime-routing-operator-surface`:
  - Added a first-class `Router` section and route family to the runtime shell, covering overview, config, candidates, decisions, and request-keyed decision detail
  - Added structured `/api/role-model/router/*` backend surfaces plus typed frontend client fetchers so Router pages no longer scrape raw request bundles inline
  - Added unified local-plus-remote candidate inventory, routing provenance/config visibility, explainable decision ledger/detail, and explicit Observe handoff
  - Fixed the QA launcher so live runtime/browser proof uses the complete `testdata/router-runtime/fixtures` bundle and exposes the Router APIs during QA startup
  - Fixed a Router decision-detail race where stale async responses could overwrite the active request after fast navigation
  - Focused validation green: `runtime-host-bridge` test/build, `runtime-ui` build, `runtime-ui` suite set in single-worker mode; live browser QA proved Router overview/config/candidates/decisions/detail and Observe cross-link against seeded request `req-run32-router-003`
  - Root `ci:check` still reproduces the inherited Phase 0 formatter-drift failure signature in `biome check .`

- Run 38 (Local Model Roles — Peer / Llama-swap Split) completed on branch `recursive/38-local-model-roles-peer-llama-swap-split`:
  - Local pillar now uses **Choose local backend** (`/app/local/choose`), **Peer models** (`/app/local/peer-models`), and **Llama-swap models** (`/app/local/llama-swap/models`); legacy `/app/local/models` redirects to the chooser; llama-swap satellite routes live under `/app/local/llama-swap/*` with legacy redirects from `/app/local/swap`, `/app/local/policy`, `/app/local/logs`, `/app/local/matrix`
  - `runtime-host-bridge` exposes split local model APIs; peer roles persist on provider-account `modelRoleBindings` and survive `syncLocalPeerState` merge; llama-swap roles persist on `model-overrides.json` `roleIds`
  - `local-model-role-bindings.ts` feeds `buildRuntimeRoleBindings` so assigned local roles appear in router candidates and endpoint readback
  - `provider-account` accepts `modelRoleBindings` on peer accounts with empty `allowedModels` (wildcard semantics)
  - Packaged runtime on `:3456` validated: peer `lfm2.5-8b-a1b` with `general.chat` + `tool.agent`, `probe-downstream-ingress.py` 0 BRIDGE_CRASH, browser QA PASS for split pages and legacy redirects
  - Addendum 01: `llama-swap-setup.ts` scaffold + runtime-config insert action + llama-swap setup hints/modal when config not operational; verified on peer-only operator config without enabling llama-swap inference

- Run 39 (Runtime Session Rehydration + Model Inventory) completed on branch `recursive/39-runtime-session-rehydration-model-inventory`:
  - Session bootstrap rehydrates `operator-intent.json` and SQLite `runtime_endpoints` on startup without Providers UI revisit; corrupt manifest surfaces bootstrap diagnostics
  - `/api/role-model/runtime/summary` and `/healthz` expose bootstrap stages, inventory summary, and alias drift; `/app/system/session-readiness` operator surface added
  - Inventory-first alias pool resolution in `routable-inventory.ts` with config-save validation and drift warnings
  - Addendum routing remediation: `runtime-routing-model.ts` hides non-routable fixture routing-model IDs in diagnostics (R10); ask-mode difficulty uses last `user` turn for Craft payloads (R11/R15)
  - Focused validation green: 39 run-39 bridge tests + `validate-ui` + `validate-restart-rehydration`; packaged restart drill deferred where operator peer process unavailable

- Run 40 (Catalog Economics + Moonshot Consolidation) on branch `recursive/40-catalog-economics-moonshot-consolidation`:
  - Catalog `TokenEconomics` + canonical Kimi map; catalog-derived `cost_per_1k_tokens_est` for routing/budget
  - Protocol-routing requires catalog; router cost metric source `"catalog"`; telemetry cost excluded
  - Bridge hides `moonshotai` from provider list; Moonshot variant dedupe; diagnostics `catalogEconomics`
  - Strict TDD RED/GREEN evidence; automated floor green; packaged `:3456` drill deferred; R8 authProfile partial

- Run 41 (Dashboard Latency Summary Display) merged on branch `recursive/41`:
  - Dashboard overview Latency card shows **average** as primary value and **p95** as secondary context (UI-only; `summarizeTelemetryStats` in `view-models.ts`)
  - Backend `RuntimeTelemetrySummary` contract unchanged; comparison rows already showed both metrics
  - All 90 runtime-ui tests green; no new architectural decisions

- Run 42 (Provider Kind + Craft Ask Routing) on branch `recursive/42-provider-kind-craft-ask-routing`:
  - Shared `resolveValidationProviderMetadata` merge makes operator-facing provider metadata match `validateProviderAccounts` for all catalog∩LiteLLM overlap ids (19 historically broken)
  - Craft declared-tools ask-mode: declared tools without active `tool`/`tool_calls` usage use last-user-turn rubric with `toolCount: 0` for difficulty scoring
  - Packaged `:3456` DeepSeek verification: connect (`provider-openai`), chat pong, quick capability benchmark 60/60
  - 48/48 targeted bridge tests green

- Run 43 (Benchmark Routing Display) on branch `recursive/43-benchmark-routing-display`:
  - Closes benchmark → profile → routing → UI loop: per-mode summaries, routing quality aggregator with `hardBlend`, candidate enrichment, dashboard latency detail, benchmark latency visibility, global benchmark clear
  - **Addendum 01:** Models → Benchmark dual full+quick inside each model card (not standalone section); run history at page bottom; `benchmark_mode` on sqlite persist; Q5′ `hardBlend` on candidates after quick re-run on SEA
  - **Addendum 02:** `credential-ref-env.ts` blocks inline `sk-` in sqlite `credential_ref`; external runtime config uses `${DEEPSEEK_API_KEY}` with host env var
  - Worktree evidence: `evidence/logs/green/sp43-*.log`, addenda under `/.recursive/run/43-benchmark-routing-display/addenda/`; memory episode `/.recursive/memory/episodes/run-43-benchmark-routing-display.md`
  - Ready for merge to `main`

- Run 44 (Kimi K2.7 Code Catalog) on branch `recursive/44-kimi-k2.7-code-catalog`:
  - Added `moonshot/kimi-k2.7-code` to operator Moonshot catalog (Open Platform + Kimi Code variants) via models.dev refresh, supplement, alias, export
  - `deriveCapabilities` maps `structured_output`; LiteLLM fixture row for k2.7
  - Catalog 16/16 tests + economics validation + bridge provider tests green
  - Packaged SEA SHA256 `9a3e4888a877236de8ccb1bed12ef1f0306d357719ec225ed26f786467e7079f`; `:3456` providers smoke PASS
  - Worktree: `.worktrees/44-kimi-k2.7-code-catalog`; ready for merge to `main`
- GitHub Actions validation in `/.github/workflows/ci.yml` is now phase-attributed instead of collapsing into one opaque parity step: install, lint, typecheck, test, docs build, and workspace validation fail independently so GitHub-only regressions are diagnosable from the failing phase.
- `/.github/workflows/docs-site-deploy.yml` now always builds docs on pushes and pull requests, uploads the static site artifact for inspection, and only deploys to Cloudflare Pages on non-PR events after explicit secret and account guards.
- `/.github/workflows/build-binaries.yml` now treats release publication as a single tag-gated publish flow: matrix builds produce per-platform archives plus attestations, then one final publish job assembles archives, `install.sh`, `install.ps1`, and `SHA256SUMS.txt` before creating or updating the GitHub release.
- GitHub release notes are now layered: `/.github/release.yml` controls GitHub-generated note categories, while `/scripts/generate-recursive-release-changelog.mjs` appends a repo-authored `Recursive Change Log` synthesized from changed `03-implementation-summary`, `06-decisions-update`, and `07-state-update` artifacts in the tagged commit range, with fallback to direct `/.recursive/STATE.md` and `/.recursive/DECISIONS.md` diff bullets.
- Run 63 (Router Backend Regression and Telemetry Surface Hardening) completed on branch `recursive/63-router-backend-regression-and-telemetry-surface-hardening`:
  - The shared runtime testing contract now includes `runtime:test-router`, a CI-safe router regression lane that runs the host-bridge `test:router` subset, `@role-model-router/trace` and `@role-model-router/usage` package tests, `runtime:validate-routing`, and `runtime:validate-observability`; `/.github/workflows/ci.yml` runs that lane as its own step alongside `runtime:test-critical`.
  - `@role-model-router/trace` and `@role-model-router/usage` now expose package-level Vitest floors for deterministic temp-dir round-trip and linkage checks, and trace artifact readback tolerates a missing `trace-events.jsonl` when no events were emitted.
  - Dashboard, Observe Requests, and Observe Routing now share stale-refresh recovery logic: background analytics failures may reuse prior chart data only with a visible cached-data warning plus bounded structured diagnostics, and a later successful refresh clears the stale-chart warning instead of leaving degraded state stuck on screen.
  - The rebuilt-runtime request-analytics regression now proves filter narrowing, query-param restoration after reload, and request-detail drill-in with uniquely seeded telemetry rows against the persistent QA runtime instead of relying on render-only assertions.
- Run 64 (Observed Data Decay Policy Recalibration) completed on branch `recursive/64-observed-data-decay-policy-recalibration`:
  - The canonical observed-data config surface now uses `metric_decay_percent_per_day` for `latency` and `throughput` only; canonical render/readback no longer advertises live decay knobs for quality, reliability, or cost.
  - Router-core now applies a 10%-per-day retained-deviation loss curve only to latency and throughput. Fresh samples reset that age calculation; benchmark or measured quality, measured reliability, and measured cost no longer drift toward neutral solely because the evidence is old.
  - Effective-metric diagnostics now explicitly distinguish time-decayed metrics from pass-through metrics through freshness-source and time-decay-applied receipts, while throughput-SLA and benchmark precedence remain separate routing boundaries.
  - New RED-first host-bridge, router-core, and protocol-routing regression coverage now protects the repaired observed-data policy across config truth, route scoring, and local-vs-remote outcomes.
- Run 51 (Runtime Testing Architecture and Regression Matrix) completed on branch `recursive/51-runtime-testing-architecture-and-regression-matrix`:
  - The repository now has a durable runtime testing architecture defined in `/docs/architecture/10-runtime-testing-architecture.md` with a 6-layer taxonomy (unit, integration, validator, browser E2E, rebuilt-runtime, packaged-runtime), named root commands, TDD discipline, and deterministic vs credential-bearing verification guidance.
  - `/docs/operations/04-runtime-testing-matrix.md` provides a changed-path regression matrix mapping touched subsystems to minimum verification tiers, CI tiering (fast unit, critical regression, full package, browser E2E, rebuilt-runtime QA, packaged-runtime), and extension rules for new subsystems.
  - Config-driven test discovery is now in place: `role-model-router/apps/runtime-host-bridge/vitest.config.ts` owns host-bridge include/exclude patterns, and `role-model-router/apps/runtime-ui/vite.config.ts` declares `test.include` patterns; both package `test` scripts now use `vitest run` so all checked-in tests are discoverable by default.
  - Named root commands added: `runtime:test-critical` (focused host + UI tests plus `runtime:validate-ui` and `runtime:validate-observability`), `runtime:test-validators` (UI, observability, tools, catalog-economics), `runtime:test-browser` (Playwright rebuilt-runtime browser regression), `runtime:test-full` (critical regression plus browser E2E).
  - `runtime:validate-observability` now points to a distinct `validate-observability.ts` entrypoint that reuses `runRuntimeUiValidation` with a temporary runtime config, instead of being a silent alias of `runtime:validate-host`.
  - `runtime:test-critical` is now part of the CI workflow in `/.github/workflows/ci.yml` and the root `ci:check` script.
  - A Playwright browser E2E harness is now checked in at `role-model-router/apps/runtime-ui/playwright.config.ts` and `role-model-router/apps/runtime-ui/e2e/runtime-shell.spec.ts`; it builds the runtime UI, starts a seeded QA server on port 3462 via `RUNTIME_QA_PORT` env, and exercises the providers page (`moonshot.personal.primary` with `api-key-static` and `Active endpoints: 1`) plus the session-readiness page (`Bootstrap status`, `Lifecycle authority`, `Execution mode`, `Routable endpoints`).
  - `role-model-router/apps/runtime-ui/app/routes/providers.tsx` now includes `data-testid` stable selectors on provider maintenance card divs for future E2E tests.
  - `role-model-router/apps/runtime-host-bridge/scripts/start-for-qa.ts` now accepts `RUNTIME_QA_PORT` env for port isolation.
  - Orphaned test issues fixed: `backend-unified-runtime-config.test.ts` routing bootstrap tests have 60s timeouts; `validate-tools.test.ts` expectations corrected to match current runtime behavior; `theme.test.ts` key name fixed to `"role-model-runtime-theme"`.
  - All test suites green: host-bridge 47 files / 383 tests, runtime-ui 20 files / 190 tests, critical regression 80+88 tests + validators, browser E2E 1 test.
- Run 52 (Codex Subscription Benchmark Tool Path Fix) completed on branch `recursive/52-codex-subscription-benchmark-tool-path`:
  - The Codex Subscription benchmark tool path in `role-model-router/apps/runtime-host-bridge/src/index.ts` now uses `createRequestScopedToolRegistry(codexDynamicTools)` (in-memory, no file reads) instead of `createRuntimeToolRegistry(options.repoRoot, currentRegistry, networkFetcher)` (which reads `testdata/router-runtime/mcp-connectors.json`) at the Codex call site (line 12916).
  - `createRequestScopedToolRegistry` is now exported for direct unit testing.
  - `createRuntimeToolRegistry` is preserved for the non-Codex hosted-tools continuation path (line ~13152).
  - 6 new tests added: registry unit test, executeToolCalls integration, buildCodexDynamicTools compatibility, no-FS-access invariant, non-tool regression guard, packaging regression guard.
  - All test suites green: lint 0 errors, build pass, full test pass, test:critical 80 tests.
  - Live benchmark on packaged runtime: 12/12 cases completed without ENOENT on `mcp-connectors.json`; tool-bearing cases executed successfully.
- Run 53 (Runtime Telemetry Analytics Contract Hardening) completed on branch `recursive/53-runtime-telemetry-analytics-contract-hardening`:
  - `/api/role-model/telemetry/query` now returns a backend-owned analytics contract with applied query metadata, slice metadata, metric support, dimension support, explicit empty arrays for valid empty slices, and full-slice aggregation that no longer inherits the request-ledger default 50-row cap.
  - Runtime-host telemetry analytics and request-ledger reads now share overlapping filter semantics for date/source/provider/endpoint/model/role/operation/status/routing/difficulty where supported, while preserving separate ledger pagination semantics.
  - Runtime UI telemetry charts now consume shared semantic chart states for loading, refreshing, empty, unsupported, partial, truncated, error, and populated cases instead of deriving truth from bucket length alone.
  - Horizontal ranking charts now use bottom legends for long model/endpoint labels and a concrete 280px plot region so Recharts bars render reliably after the legend move.
  - `/docs/architecture/11-runtime-ui-telemetry-graph-matrix.md` is the current graph matrix architecture reference for telemetry storage, analytics contract, metric/dimension semantics, frontend chart-state handling, and route ownership.
  - Packaged-runtime manual QA passed on `2026-06-21`; temporary QA telemetry rows inserted for populated browser review were deleted after sign-off and verified at `remainingRows: 0`.
- Run 54 (Alias Capability Discovery Contract) completed on branch `recursive/54-alias-capability-discovery-contract`:
  - `/api/role-model/downstream/openai` is now the rich downstream OpenAI-compatible discovery contract for exact models and aliases, with contract versioning, freshness revision, sanitized endpoint ids, Pi mapping hints, safe/max limits, declared/routable layers, modalities, tools/function-calling, structured output, reasoning, and advisory caching.
  - Runtime model metadata now uses a shared capability resolver; `chatgpt/gpt-5.4` and related Codex Subscription runtime IDs resolve through canonical GPT metadata while preserving the public runtime model id.
  - Alias discovery is derived on every read from the current registry, catalog, runtime alias config, and effective routable inventory. Endpoint/model additions, routing-strategy alias regeneration, execution-mode changes, and health/readiness changes update discovery through those inputs.
  - Every configured downstream alias receives a discovery record, including aliases whose current `routable` pool is empty; empty pools keep declared configured targets visible and expose empty `routable` sets rather than disappearing from the model list.
  - `/v1/models` now also exposes compact additive capability metadata for downstream consumers that start from the OpenAI-compatible model-list URL, including `context_window`, `max_tokens`, Pi-compatible `input`, full modalities, capability names, `role_model.discovery_url`, and `role_model.capability_revision`.
  - Chat Completions and Responses share request capability inference. Image, video, function-tool, structured-output, and explicit reasoning-control requirements narrow alias endpoint pools before scoring and return stable `no_eligible_target` errors when no target can satisfy the request.
- Run 58 (Role-Model Taxonomy V1 Benchmark + Telemetry) implemented on branch `recursive/58-role-model-taxonomy-v1-benchmark-telemetry` in worktree `.worktrees/58-taxonomy-benchmark-telemetry`:
  - Added 4 taxonomy-aware benchmark/telemetry schemas under `schemas/role-model/taxonomy/`: benchmark-suite, benchmark-run, benchmark-result, telemetry-taxonomy-event. All schemas include `additionalProperties: true` for forward compatibility. Schemas validated with AJV against sample data.
  - Tagged 15 routing-capability benchmark cases with canonical taxonomy metadata covering 6 roles and 6 task types including coder.review (2 cases), researcher.compare_sources, support.ticket.reply, and data.schema.review.
  - Extended `BenchmarkSummarySubject` with 6-dimension taxonomy score aggregation: byRole, byTask, byVariant, byCapability, byModality, byToolClass. End-to-end pipeline: runner extracts `taxonomy_tags` from cases → persisted result includes `caseTaxonomyTags` → summary API computes per-dimension aggregates via `computeTaxonomyAggregates`.
  - Added per-task benchmark scoring to `getQualityMetric` in router: blends `overallScore` (0.7 weight, configurable) with task-specific score (0.3 weight). Added `benchmark_task_score` to quality metric raw output. Added `BENCHMARK_TASK_SCORE` and `TELEMETRY_TASK_PERFORMANCE` to `router-decision.schema.json` selection_reasons enum.
  - Added `extractTaxonomyDimensions` to `@role-model/protocol-types` as canonical single source. Extended `RuntimeObservationBundle` with `taxonomyDimensions` (7 extracted fields) and `privacyReceipt` (samplingRate, retentionTtlHours, retainUntil). Added `retain_until_ms` indexed column to `runtime_observations` with backward-compatible migration.
  - Extended telemetry analytics with `taxonomyRoleId` and `taxonomyTaskType` dimensions: extraction from persisted bundle → enrichment in `BridgeTelemetryRequestRecord` → filter/breakdown in query API. Frontend types and observe-routing filters extended with taxonomy dimension inputs, URL-addressable via `useSearchParams`.
  - Benchmark UI: taxonomy filter SectionCard in `control-benchmark.tsx` with role/task/capability dropdowns. Model detail: advisory benchmark score label and telemetry rollup DisclosureSection with `fetchModelTelemetryRollup` API.
  - Created `TAXONOMY_BENCHMARK_DIMENSIONS` const registry in protocol-types. Made router blend weights and telemetry advisory thresholds configurable via `ObservedDataConfigRecord`. Fixed difficulty classifier fallback: when `difficultyClassifier.modelId` is null, falls back to controller's modelId (addendum SP-C1).
  - 257+ tests across 7 packages pass. 21 new tests with RED→GREEN evidence. All 3 packages build clean. Runtime and Pi packages rebuilt and deployed. Live QA confirmed: 15 tagged cases, 6-dimension taxonomy scores populated, taxonomy telemetry dimensions supported in API. Remote execution blocked by `litellm` binary availability in QA environment.
  - Pi can discover the configured role-model provider aliases from `/v1/models`: all `15` aliases list with `262.1K` context, `128K` max output, thinking enabled, and image support, while concrete DeepSeek models still correctly report no image input support. Agent-operated Pi alias-matrix QA sent at least three prompts to every alias with zero role-model telemetry failures.
- Run 59 (Observe Taxonomy Analytics Completion) completed on branch `recursive/59-observe-taxonomy-analytics-completion` in worktree `.worktrees/59-observe-taxonomy-analytics-completion`:
  - Completed the richer taxonomy telemetry layer that run 58 left partial: canonical extraction now preserves original role/task hints, normalized role/task values, group/variant/capability/modality/tool-class dimensions, alternatives, source/confidence metadata, and mixed-version compatibility semantics.
  - Observe Requests and Observe Routing now render richer taxonomy graph inventories and controls through the shared runtime UI design system, with URL-backed filters for group, role, task, variant, capability, modality, and tool class dimensions.
  - `/app/models` now shows richer taxonomy telemetry rollups plus benchmark-advisory role/group evidence, and request detail now shows structured original-hint, normalized-classification, derived-analytics, telemetry-handling, and cost-audit sections instead of relying on raw observation disclosure alone.
  - Runtime telemetry performance was repaired in the live rebuilt runtime by persisting richer taxonomy dimensions directly into `runtime_telemetry_records`, backfilling existing rows, and removing Observe analytics/request-list dependence on reparsing large `runtime_observations.observation_json` bundles. Live rebuilt-runtime timings moved from minute-scale or lock-failure behavior to roughly `3.6s` for `/app`, `1.7s` for `/app/observe/requests`, `1.1s` for `/app/observe/routing`, and about `250-310 ms` for representative analytics POSTs.
  - `@try-works/pi-role-model` now exposes runtime-owned `/role-model requests` and `/role-model explain latest` flows, refreshes effective taxonomy after setup and alias refresh, honors `ROLE_MODEL_ENDPOINT` for runtime inspection, and remains read-only with no runtime-process or credential ownership.
  - A late rebuilt-runtime benchmark proof found and repaired a real mixed-data router bug where measured quality shadowed benchmark task/role/group quality. Live rebuilt-runtime receipts now emit `BENCHMARK_TASK_SCORE`, `BENCHMARK_ROLE_SCORE`, and `BENCHMARK_GROUP_SCORE` when appropriate, while measured latency/throughput/reliability remain advisory non-quality inputs.
  - Agent-operated manual QA passed on rebuilt runtime `:3462`, and the rebuilt run-59 runtime was later restarted on `:3456` with Pi `0.80.2` for direct operator testing.
