Run: `/.recursive/run/89-codex-role-model-package/`
Phase: `01.5 Root Cause`
Addendum: `02` (`premature-tool-narration-stop`)
Status: `DRAFT`
Workflow version: `recursive-mode-audit-v2`
Inputs:
- Desktop workspace `C:\Users\erikb\Documents\Codex\2026-08-06\stress-test-multi-hop-routing-tools-2`
- Runtime `:3458` `/api/role-model/requests` audits (`runtime-3458-stall-analysis.json`, `runtime-3458-stall-audit-0717.json`)
- Adapter `last-bridge-hop.json` traces `e92db7db…`, `9dcca203…`, `714e27e3…`
Outputs:
- `/.recursive/run/89-codex-role-model-package/addenda/01.5-root-cause.premature-tool-narration-stop.addendum-02.md`
Scope note: Adapter-only diagnosis of DeepSeek message-only premature stops that narrate the next tool without emitting `tool_calls`. Distinct from SERP-dump (addendum 01) and missing `response.id` (01b).

## Error Analysis

**Error Message:** (user-visible) turn ends after prose such as “Now writing `stress-lab/final-report.md`…” with no subsequent tool UI; file never appears.
**Stack Trace:** N/A (logical completion)
**File:Line:** Adapter forwards message-only JSON/SSE as completed; no recovery in `finalizePayloadForCodex`
**Key Insight:** Upstream `finishReason=stop` with empty `toolCalls` + short assistant text that *announces* a tool is treated as a successful final answer.

## Reproduction Verification

**Steps:**
1. Multi-step Desktop/CLI stress prompt with shell + apply_patch available.
2. Model completes several `tool_calls` hops successfully.
3. Next hop returns short narration of the next write/shell step without a tool call.
4. Desktop/CLI shows turn complete; artifact missing.

**Reproducible:** Yes (observed ≥3 times in tools-2 thread: 07:05:12, 07:08:42, 07:15:02 local)
**Frequency:** High on long multi-step turns after nudges
**Deterministic:** Pattern-stable; exact phrase varies

## Multi-Layer Evidence

**Layer 1: Workspace** — `tool-discipline.md` present; `final-report.md` / `STRESS_TEST_COMPLETE` missing after narration of write.

**Layer 2: Adapter** — `bridgeTraceId=714e27e3…`, restore all-zero, `durationMs≈2140`, outcome ok.

**Layer 3: Runtime `:3458`** — `req-9474d117…`, `finishReason=stop`, `toolCalls=0`, `tokens_out=43`, `tokens_in≈28295`, `toolingUsed=false`, intent `data.quality.audit`.

**Failure Boundary:** Model stop-without-tools → adapter accepts as final → client ends turn.

## Confirmed Root Cause

DeepSeek v4-flash (via `baseline.remote-only`) intermittently ends multi-step tool turns with `finishReason=stop` and brief prose that narrates the *next* tool action instead of emitting `function_call` / shell / apply_patch. The adapter has no detector/continue for this case (unlike web_search fulfill), so Codex treats the message as completion.

Secondary: heuristic intent `data.quality.audit`; growing context; Desktop reconnect can compound but is not required to explain these stops.

## Hypothesis

Bounded adapter auto-continue when message-only output matches tool-narration patterns and tools were available will recover into a real tool hop. Anti-narration guidance reduces incidence.

## Out of Scope

SERP quote quality; Desktop reconnect transport; router intent retrain; native reasoning UI.

## Audit

- Audit Execution Mode: self-audit (live workspace + runtime request evidence already captured)
- Audit: PASS
