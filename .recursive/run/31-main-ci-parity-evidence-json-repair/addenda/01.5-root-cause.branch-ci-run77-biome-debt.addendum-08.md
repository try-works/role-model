Run: `/.recursive/run/31-main-ci-parity-evidence-json-repair/`
Phase: `01.5 Root Cause Analysis`
Status: `LOCKED`
Addendum: `08`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run76-biome-debt.addendum-06.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run77.red.log`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run77-repro.red.log`
- User direction: continue with the fixes
Outputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run77-biome-debt.addendum-08.md`
Scope note: This addendum records the next masked branch-CI blocker exposed after the run-76 remediation landed on the branch.

## TODO

- [x] Re-read the new run-77 branch-CI evidence and local reproduction log
- [x] Bound the newly exposed blocker to the actual run-77 file set
- [x] Distinguish this new layer from the earlier run-76 runtime-host-bridge/runtime-ui cleanup
- [x] Record the next fix direction for Phase 2
- [x] Complete the audited sections and gates

## Error Analysis

**New failing surface:** GitHub Actions run `#77` (`25700742548`) still fails in `validate -> CI parity`, but the run-76 blocker is gone. The newly exposed file set in `phase4-branch-ci-run77.red.log` is:

- `role-model-router/vendor/llama-swap/ui-svelte/src/main.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/speechApi.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/sdApi.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/rerankApi.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/modelUtils.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/markdown.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/markdown.test.ts`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/imageApi.ts`
- `role-model-router/apps/runtime-host-bridge/src/index.ts`

**Observed failure summary:** `phase4-branch-ci-run77.red.log` records:
- `format` failures across the vendored Svelte helper files and `markdown.test.ts`
- `organizeImports` in `src/main.ts`, `src/lib/sdApi.ts`, and `src/lib/markdown.ts`
- explicit fixable lint in vendored Svelte helpers:
  - `src/main.ts` `lint/style/noNonNullAssertion`
  - `src/lib/markdown.ts` `lint/correctness/noUnnecessaryContinue`
  - `src/lib/markdown.ts` `lint/style/useTemplate`
  - `src/lib/markdown.ts` `lint/style/noParameterAssign`
  - `src/lib/markdown.test.ts` `lint/style/useTemplate`
- explicit `lint/style/noNonNullAssertion` in `role-model-router/apps/runtime-host-bridge/src/index.ts` at lines `1437` and `1925`
- `Checked 457 files in 722ms. No fixes applied.`
- `Found 334 errors.`

**Key insight:** the run-76 remediation worked. GitHub CI is now failing on a smaller vendored Svelte formatting/lint layer plus two non-null assertions in runtime-host-bridge `src/index.ts`. There is no config mismatch and no tool crash in the run-77 blocker.

## Reproduction Verification

**Steps:**
1. Push commit `52548e5169692d9b1ded080410214e362a6dbba2`.
2. Wait for GitHub Actions run `25700742548`.
3. Inspect `phase4-branch-ci-run77.red.log`.
4. Re-run `corepack pnpm exec biome check` over the exact 9-file set listed above.
5. Confirm the local run fails on the same bounded file surface.

**Reproducible:** Yes
**Frequency:** 1 of 1 GitHub runs and 1 of 1 focused local reproductions
**Deterministic:** Yes

## Recent Changes Analysis

**Relevant change:** the run-76 remediation committed in `52548e5` cleared the prior runtime-host-bridge/runtime-ui formatter layer.

**Likely culprit:** the run-76 fix was correctly bounded to its 14-file set. Because CI still executes `biome check .` across the whole repository, the next masked Biome layer surfaced immediately after the branch got past run `76`.

## Evidence Gathering

- Layer 1: GitHub Actions `CI parity`
  - Input: branch commit `52548e5169692d9b1ded080410214e362a6dbba2`
  - Output: 9-file vendored-Svelte/runtime-host-bridge failure set with ordinary format / organize-import diagnostics plus a handful of explicit lint findings
  - Status: broken
- Layer 2: focused local repro
  - Input: the same 9 files from the GitHub log
  - Output: the local run fails on the same bounded file set
  - Status: broken
- Layer 3: blocker class comparison
  - Input: run `76` versus run `77` evidence
  - Output:
    - run `76`: runtime-host-bridge source/tests and runtime-ui formatter layer
    - run `77`: vendored `llama-swap` Svelte helper layer plus two runtime-host-bridge `src/index.ts` non-null assertions
  - Status: confirmed
LockedAt: `2026-05-11T22:23:42Z`
LockHash: `f9c4d7ee5a20c82b06576cd4ffa8429c486ff9e94a6e5629e010860de038ce01`

**Failure boundary:** repo-wide `biome check .` -> next masked vendored-Svelte/runtime-host-bridge formatter/lint layer

## Data Flow Trace

1. The run-76 remediation pushes branch CI past the earlier runtime-host-bridge/runtime-ui debt layer.
2. `pnpm run lint` still executes `biome check .` over the whole repository.
3. Biome now reaches a new bounded group of vendored Svelte files and one runtime-host-bridge source file and reports ordinary format / organize-import debt plus explicit lint findings.
4. The run exits with standard lint failure `1`, not a tool crash.

**Corrected root cause for the remaining CI failure:** run 31 now faces a fifth masked Biome layer: 9 vendored-Svelte/runtime-host-bridge files outside the earlier run-76 slice still need routine formatting, organize-import cleanup, and a few small non-null/template/parameter-assignment repairs.

## Pattern Analysis

| File class | Example | Pattern |
| --- | --- | --- |
| Vendored Svelte entry/helper files | `src/main.ts`, `src/lib/sdApi.ts`, `src/lib/imageApi.ts` | ordinary formatting and import-order debt |
| Vendored markdown helpers | `src/lib/markdown.ts`, `src/lib/markdown.test.ts` | formatter/import debt plus small explicit lint repairs |
| Runtime-host-bridge source | `apps/runtime-host-bridge/src/index.ts` | two explicit `noNonNullAssertion` sites in otherwise stable code |

## Hypothesis Testing

### Hypothesis 1

**Statement:** run `77` is no longer blocked by the run-76 file set.

**Rationale:** the new GitHub log contains a different file set and no references to the run-76 runtime-ui or runtime-host-bridge test files.

**Test:** inspect the run-77 GitHub CI log after pushing `52548e5`.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run77.red.log`

### Hypothesis 2

**Statement:** the remaining blocker is bounded to a concrete 9-file vendored-Svelte/runtime-host-bridge set.

**Rationale:** the GitHub log names a finite set of paths, and the local focused repro fails on the same set.

**Test:** rerun Biome on only the run-77 files and inspect the failure surface.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run77-repro.red.log`

### Hypothesis 3

**Statement:** the new blocker is ordinary Biome debt plus a few explicit lint hotspots, not another crash or config mismatch.

**Rationale:** the run-77 log reports only `format`, `organizeImports`, and explicit lint findings, then exits `1`.

**Test:** compare run-77 diagnostics with the earlier run-76 evidence and inspect the flagged `src/index.ts`, `src/main.ts`, and `src/lib/markdown.ts` sites.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run77.red.log`

## Root Cause Summary

**Corrected Root Cause:** after the run-76 remediation removed the previous runtime-host-bridge/runtime-ui formatter layer, branch CI exposed a fifth masked Biome layer: 9 vendored-Svelte/runtime-host-bridge files still contain ordinary formatting/import debt plus a few small non-null/template/parameter-assignment repairs.

**Location:** the 9 run-77 paths listed in `phase4-branch-ci-run77.red.log`, with explicit runtime-host-bridge lint hotspots at `src/index.ts:1437` and `src/index.ts:1925`

**Detailed Explanation:** the branch keeps moving forward one masked lint layer at a time. The run-76 slice is no longer failing, which confirms that fix. The next remaining failure is again standard Biome debt, but now concentrated in vendored `llama-swap` Svelte helpers plus two runtime-host-bridge assertions. No new config change is required, and there is no tool crash to work around. The required next step is a bounded cleanup of the run-77 file set only, followed by another branch-CI rerun.

**Fix Strategy:** apply Biome-managed formatting and organize-import cleanup to the run-77 file set, replace the two `src/index.ts` non-null assertions with local variables or guarded lookups, repair the small vendored `markdown.ts` / `markdown.test.ts` lint findings directly, rerun the focused file set locally, and rerun branch CI.

**Test Plan:** preserve run `77` as RED evidence, capture a focused local green Biome run over the 9-file set, rerun GitHub Actions CI, and only widen again if a later run surfaces a new explicit blocker.

## Traceability

- `R4` -> this addendum explains why parity is still blocked after the run-76 remediation
- `R5` -> the run keeps widening only because GitHub CI continues to expose new confirmed blockers while the user has asked to continue fixing them

## Effective-Input Rule For Later Phases

Until superseded by a later locked addendum, later phases for run 31 must treat this file as an authoritative effective input together with:

- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run76-biome-debt.addendum-06.md`

This addendum adds the run-77 masked vendored-Svelte/runtime-host-bridge Biome debt layer to the run.

## Coverage Gate

- [x] New failing surface identified
- [x] File scope bounded to the GitHub-reported and locally reproduced set
- [x] New blocker class distinguished from the earlier run-76 cleanup slice
- [x] New fix direction defined

Coverage: PASS

## Approval Gate

- [x] The remaining blocker is concrete and non-speculative
- [x] Scope expansion is grounded in continued explicit user direction
- [x] Later phases can plan the bounded repair without reopening earlier RCA

Approval: PASS

## Audit Verdict

- Audit summary: the remaining branch-CI blocker is now root-caused as a fifth masked Biome layer consisting of vendored-Svelte/runtime-host-bridge formatter/import debt plus a few explicit lint hotspots across 9 files.
Audit: PASS
