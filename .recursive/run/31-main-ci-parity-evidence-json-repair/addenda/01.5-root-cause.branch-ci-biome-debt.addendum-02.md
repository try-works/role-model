Run: `/.recursive/run/31-main-ci-parity-evidence-json-repair/`
Phase: `01.5 Root Cause Analysis`
Status: `LOCKED`
Addendum: `02`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.multiple-malformed-run15-json.addendum-01.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run73.red.log`
- User scope decision: expand run 31 to include the broader Biome formatting cleanup now
Outputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-biome-debt.addendum-02.md`
Scope note: This addendum records the next root cause exposed after the malformed-JSON repair cleared the original parse blocker on branch CI.

## TODO

- [x] Re-read the locked RCA and the new branch-CI failure evidence
- [x] Bound the newly exposed blocker to the actual GitHub-reported file set
- [x] Distinguish the new failure class from the already-repaired malformed JSON class
- [x] Record the new fix direction for later phases
- [x] Complete the audited sections and gates

## Error Analysis

**New failing surface:** GitHub Actions run `#73` (`25698259660`) still fails in `validate -> CI parity`, but the failing lint step is now reporting broad Biome diagnostics rather than malformed JSON parse errors.

**Observed failure summary:** `phase4-branch-ci-run73.red.log` records:
- `> biome check . && corepack pnpm run lint:rust`
- 13 unique failing file paths
- `Found 412 errors.`

**Key insight:** the malformed JSON blockers were real and are now cleared. The branch CI is failing on a different, previously masked baseline: tracked Biome formatting and fixable lint debt across 13 files.

## Reproduction Verification

**Steps:**
1. Push the malformed-JSON repair branch.
2. Wait for GitHub Actions run `25698259660`.
3. Inspect `phase4-branch-ci-run73.red.log`.
4. Confirm the reported files span testdata JSON, recursive evidence JSON, docs-site TSX, and vendored llama-swap UI files.

**Reproducible:** Yes
**Frequency:** 1 of 1 branch CI runs after the malformed-JSON repair exposed the same 13-file failure set
**Deterministic:** Yes

## Recent Changes Analysis

**Relevant change:** the malformed run-15 JSON class was repaired first. Only after that repair did GitHub Actions reach the next layer of repo-wide Biome debt.

**Likely culprit:** the repository already contained a 13-file set of formatting and fixable lint issues that the earlier malformed JSON parse failure masked.

## Evidence Gathering

- Layer 1: GitHub Actions `CI parity`
  - Input: repaired branch commit `e6ad8608d17c51d12a3e14591364295bb44f5828`
  - Output: Biome lint fails with 412 errors
  - Status: broken
- Layer 2: diagnostic file set
  - Input: branch CI log
  - Output: 13 unique paths:
    - `.agents/skills/ui-design-system/scripts/tests/coverage-ui.json`
    - `.recursive/run/14-router-runtime-ui-foundation/evidence/logs/verify/runtime-host-streaming-e2e.json`
    - `.recursive/run/15-unified-vendor-execution/evidence/browser/run15-real-vendor-browser-probe.json`
    - `apps/docs-site/app/components/docs-header.tsx`
    - `role-model-router/vendor/llama-swap/ui-svelte/vite.config.ts`
    - `role-model-router/vendor/llama-swap/ui-svelte/src/stores/theme.ts`
    - `role-model-router/vendor/llama-swap/ui-svelte/src/stores/playgroundActivity.ts`
    - `role-model-router/vendor/llama-swap/ui-svelte/src/stores/persistent.ts`
    - `role-model-router/vendor/llama-swap/ui-svelte/src/stores/api.ts`
    - `role-model-router/vendor/llama-swap/ui-svelte/src/routes/Playground.svelte`
    - `testdata/catalog/litellm-model-prices.json`
    - `testdata/router-runtime/fixtures/routing-model-guidance.json`
    - `testdata/router-runtime/routing-model-guidance.json`
  - Status: broken
LockedAt: `2026-05-11T21:45:07Z`
LockHash: `a23c8954e169e8f415fd22907449219cc118aa2e27aec03b0610fd4788279531`

**Failure boundary:** masked repo-wide Biome debt -> branch CI lint step once the malformed JSON parse blockers no longer short-circuit the check

## Data Flow Trace

1. The repaired branch reaches the same `pnpm run ci:check` workflow step that failed on `main`.
2. `pnpm run lint` executes `biome check .`.
3. Biome no longer stops on the malformed run-15 parse failure.
4. Biome instead surfaces the next blocking class: formatting, organize-imports, and fixable style issues across 13 tracked files.

**Corrected root cause for the remaining CI failure:** the repo has a pre-existing 13-file Biome debt that was masked by the earlier malformed JSON parse failure and now blocks the repaired branch.

## Pattern Analysis

| File class | Example | Diagnostic type |
| --- | --- | --- |
| Recursive evidence JSON | `run15-real-vendor-browser-probe.json` | `format` |
| Testdata JSON | `testdata/router-runtime/routing-model-guidance.json` | `format` |
| Docs-site source | `apps/docs-site/app/components/docs-header.tsx` | `format` |
| Vendored llama-swap UI | `src/stores/api.ts`, `Playground.svelte` | `organizeImports`, `format`, fixable style lint |

## Hypothesis Testing

### Hypothesis 1

**Statement:** the branch CI is no longer failing on malformed JSON parse errors.

**Rationale:** the new log excerpt shows Biome reporting formatting and import-order debt instead of parse diagnostics.

**Test:** inspect the branch CI failure excerpt after the malformed-JSON repair commit.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run73.red.log`

### Hypothesis 2

**Statement:** the remaining CI blocker is bounded enough to repair in the same run without widening to repo-wide arbitrary cleanup.

**Rationale:** the GitHub log names 13 concrete files rather than an unbounded codebase-wide surface.

**Test:** extract the unique diagnostic file paths from the branch CI log.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run73.red.log`

## Root Cause Summary

**Corrected Root Cause:** after the malformed run-15 JSON repairs removed the original parse blocker, GitHub Actions exposed a separate pre-existing 13-file Biome formatting and fixable-lint debt that still blocks `pnpm run lint`.

**Location:** the 13 GitHub-reported paths listed in `phase4-branch-ci-run73.red.log`

**Detailed Explanation:** run 31 successfully repaired the malformed JSON class, but that only unmasked the next failing layer in the same CI parity step. The remaining failure is not another recursive-evidence serialization problem; it is a bounded set of Biome diagnostics across testdata JSON, recursive evidence JSON, docs-site TSX, and vendored llama-swap UI files. Because the user explicitly chose to expand run 31, the run can now treat that 13-file set as the remaining blocker to green CI.

**Fix Strategy:** clean only the 13 GitHub-reported files using Biome-managed formatting and organize-import fixes plus the minimal manual edits needed for fixable style diagnostics, then rerun branch CI.

**Test Plan:** preserve run `73` as RED evidence, fix the 13-file set, capture a local focused Biome green run over those files, and rerun GitHub Actions CI.

## Traceability

- `R4` -> this addendum explains why parity is still blocked after the malformed-JSON repair
- `R5` -> the run is only widening because branch CI exposed a new confirmed blocker and the user explicitly approved expanding scope

## Effective-Input Rule For Later Phases

Until superseded by a later locked addendum, later phases for run 31 must treat this file as an authoritative effective input together with:

- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.multiple-malformed-run15-json.addendum-01.md`

This addendum adds the masked 13-file Biome debt as an in-scope blocker after the malformed-JSON repair.

## Coverage Gate

- [x] New failing surface identified
- [x] File scope bounded to the GitHub-reported set
- [x] New fix direction defined

Coverage: PASS

## Approval Gate

- [x] The remaining blocker is concrete and non-speculative
- [x] Scope expansion is grounded in explicit user direction
- [x] Later phases can plan the widened cleanup without reopening the original malformed-JSON RCA

Approval: PASS

## Audit Verdict

- Audit summary: the remaining branch-CI blocker is now root-caused as a bounded 13-file Biome debt exposed after the malformed-JSON repair.
Audit: PASS
