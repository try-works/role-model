Run: `/.recursive/run/31-main-ci-parity-evidence-json-repair/`
Phase: `01.5 Root Cause Analysis`
Status: `LOCKED`
Addendum: `10`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run77-biome-debt.addendum-08.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run78.red.log`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run78-repro.red.log`
- User direction: fix the issues locally before pushing to GitHub
Outputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run78-biome-debt.addendum-10.md`
Scope note: This addendum records the next masked branch-CI blocker exposed after the run-77 remediation landed on the branch.

## TODO

- [x] Re-read the new run-78 branch-CI evidence and local reproduction log
- [x] Bound the newly exposed blocker to the actual run-78 file set
- [x] Distinguish this new layer from the earlier run-77 vendored-Svelte/runtime-host-bridge cleanup
- [x] Record the next fix direction for Phase 2
- [x] Complete the audited sections and gates

## Error Analysis

**New failing surface:** GitHub Actions run `#78` (`25701003520`) still fails in `validate -> CI parity`, but the run-77 blocker is gone. The newly exposed file set in `phase4-branch-ci-run78.red.log` and `phase4-branch-ci-run78-repro.red.log` is:

- `role-model-router/apps/runtime-ui/app/lib/design-system.test.ts`
- `role-model-router/apps/runtime-ui/app/lib/design-system.ts`
- `role-model-router/apps/runtime-ui/app/lib/device-authorization.test.ts`
- `role-model-router/apps/runtime-ui/app/lib/device-authorization.ts`
- `role-model-router/apps/runtime-ui/app/lib/runtime-api.test.ts`
- `role-model-router/apps/runtime-ui/app/lib/runtime-api.ts`
- `role-model-router/apps/runtime-ui/app/lib/view-models.test.ts`
- `role-model-router/apps/runtime-ui/app/root.tsx`
- `role-model-router/apps/runtime-ui/app/routes/control-controller.tsx`
- `role-model-router/apps/runtime-ui/app/routes/control-models.tsx`

**Observed failure summary:** the bounded run-78 evidence records:
- `format` failures across all ten runtime-ui files
- `organizeImports` in multiple tests, `design-system.ts`, `root.tsx`, `control-controller.tsx`, and `control-models.tsx`
- explicit fixable lint in repo-owned runtime-ui code:
  - `role-model-router/apps/runtime-ui/app/root.tsx` `lint/style/useConst`
  - `role-model-router/apps/runtime-ui/app/routes/control-controller.tsx` `lint/correctness/useExhaustiveDependencies`
- local focused repro result:
  - `Checked 10 files in 34ms. No fixes applied.`
  - `Found 20 errors.`

**Key insight:** the run-77 remediation worked. GitHub CI is now failing on a smaller repo-owned runtime-ui formatter/import layer plus two small React lint hotspots. There is no vendor subtree involvement in the run-78 blocker.

## Reproduction Verification

**Steps:**
1. Push commit `409b97f`.
2. Wait for GitHub Actions run `25701003520`.
3. Inspect `phase4-branch-ci-run78.red.log`.
4. Re-run `corepack pnpm exec biome check` over the exact 10-file runtime-ui set listed above.
5. Confirm the local run fails on the same bounded file surface.

**Reproducible:** Yes
**Frequency:** 1 of 1 GitHub runs and 1 of 1 focused local reproductions
**Deterministic:** Yes

## Recent Changes Analysis

**Relevant change:** the run-77 remediation committed in `409b97f` cleared the prior vendored-Svelte/runtime-host-bridge layer.

**Likely culprit:** the run-77 fix was correctly bounded to its 9-file set. Because CI still executes `biome check .` across the whole repository, the next masked runtime-ui layer surfaced immediately after the branch got past run `77`.

## Evidence Gathering

- Layer 1: GitHub Actions `CI parity`
  - Input: branch commit `409b97f`
  - Output: 10-file runtime-ui failure set with ordinary format / organize-import diagnostics plus two explicit lint findings
  - Status: broken
- Layer 2: focused local repro
  - Input: the same 10 files from the GitHub log
  - Output: the local run fails on the same bounded file set
  - Status: broken
- Layer 3: blocker class comparison
  - Input: run `77` versus run `78` evidence
  - Output:
    - run `77`: vendored `llama-swap` Svelte helpers plus two runtime-host-bridge assertions
    - run `78`: repo-owned runtime-ui formatter/import debt plus `useConst` and `useExhaustiveDependencies`
  - Status: confirmed
LockedAt: `2026-05-11T22:53:14Z`
LockHash: `2d472fbaef977c33c2297f6ee2b1960db73c4613724ddc2f0d371ee864a81336`

**Failure boundary:** repo-wide `biome check .` -> next masked repo-owned runtime-ui formatter/lint layer

## Data Flow Trace

1. The run-77 remediation pushes branch CI past the earlier vendored-Svelte/runtime-host-bridge debt layer.
2. `pnpm run lint` still executes `biome check .` over the whole repository.
3. Biome now reaches a new bounded group of runtime-ui library, route, and root files and reports ordinary format / organize-import debt plus two explicit React lint findings.
4. The run exits with standard lint failure `1`, not a runtime-ui logic failure.

**Corrected root cause for the remaining CI failure:** run 31 now faces a sixth masked Biome layer: 10 repo-owned runtime-ui files outside the earlier run-77 slice still need routine formatting, organize-import cleanup, and two small hook/const repairs.

## Pattern Analysis

| File class | Example | Pattern |
| --- | --- | --- |
| Runtime-ui tests and library files | `app/lib/design-system.test.ts`, `app/lib/runtime-api.ts` | ordinary formatting and import-order debt |
| Runtime-ui root shell | `app/root.tsx` | formatter/import debt plus a `useConst` binding split |
| Runtime-ui control route | `app/routes/control-controller.tsx` | formatter/import debt plus `useExhaustiveDependencies` for the initial load effect |

## Hypothesis Testing

### Hypothesis 1

**Statement:** run `78` is no longer blocked by the run-77 file set.

**Rationale:** the new GitHub log contains only runtime-ui files and no references to the vendored Svelte or runtime-host-bridge files fixed in run `77`.

**Test:** inspect the run-78 GitHub CI log after pushing `409b97f`.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run78.red.log`

### Hypothesis 2

**Statement:** the remaining blocker is bounded to a concrete 10-file runtime-ui set.

**Rationale:** the GitHub log names a finite set of paths, and the local focused repro fails on the same set.

**Test:** rerun Biome on only the run-78 files and inspect the failure surface.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run78-repro.red.log`

### Hypothesis 3

**Statement:** the new blocker is ordinary Biome debt plus two explicit React lint hotspots, not a deeper runtime bug.

**Rationale:** the run-78 evidence reports only `format`, `organizeImports`, `useConst`, and `useExhaustiveDependencies`, then exits `1`.

**Test:** compare run-78 diagnostics with the earlier run-77 evidence and inspect the flagged `app/root.tsx` and `app/routes/control-controller.tsx` sites.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run78.red.log`

## Root Cause Summary

**Corrected Root Cause:** after the run-77 remediation removed the previous vendored-Svelte/runtime-host-bridge formatter layer, branch CI exposed a sixth masked Biome layer: 10 runtime-ui files still contain ordinary formatting/import debt plus a `useConst` binding and an effect dependency issue.

**Location:** the 10 run-78 paths listed in `phase4-branch-ci-run78.red.log`, with explicit lint hotspots at `app/root.tsx:46` and `app/routes/control-controller.tsx:36`

**Detailed Explanation:** the branch keeps moving forward one masked lint layer at a time. The run-77 slice is no longer failing, which confirms that fix. The next remaining failure is again standard Biome debt, but now concentrated entirely in the repo-owned React runtime UI. No new runtime feature work is required. The required next step is a bounded cleanup of the run-78 file set only, followed by another local green proof before pushing.

**Fix Strategy:** add a focused regression covering the exact run-78 file set, apply Biome-managed formatting and organize-import cleanup to the 10 files, split the immutable message binding in `app/root.tsx`, make the `control-controller` load path dependency-safe, rerun the focused local checks, then push and rerun branch CI.

**Test Plan:** preserve run `78` as RED evidence, capture a focused local RED Vitest regression over the 10-file set, capture focused local green Biome and Vitest runs over the same set, and rerun GitHub Actions CI only after the local bounded surface is green.

## Traceability

- `R4` -> this addendum explains why parity is still blocked after the run-77 remediation
- `R5` -> the run keeps widening only because GitHub CI continues to expose new confirmed blockers while the user has asked to continue fixing them locally first

## Effective-Input Rule For Later Phases

Until superseded by a later locked addendum, later phases for run 31 must treat this file as an authoritative effective input together with:

- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run77-biome-debt.addendum-08.md`

This addendum adds the run-78 masked runtime-ui Biome debt layer to the run.

## Coverage Gate

- [x] New failing surface identified
- [x] File scope bounded to the GitHub-reported and locally reproduced set
- [x] New blocker class distinguished from the earlier run-77 cleanup slice
- [x] New fix direction defined

Coverage: PASS

## Approval Gate

- [x] The remaining blocker is concrete and non-speculative
- [x] Scope expansion is grounded in continued explicit user direction
- [x] Later phases can plan the bounded repair without reopening earlier RCA

Approval: PASS

## Audit Verdict

- Audit summary: the remaining branch-CI blocker is now root-caused as a sixth masked Biome layer consisting of repo-owned runtime-ui formatter/import debt plus two explicit React lint hotspots across 10 files.
Audit: PASS
