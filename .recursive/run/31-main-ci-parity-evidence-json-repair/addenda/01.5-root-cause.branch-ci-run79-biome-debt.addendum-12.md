Run: `/.recursive/run/31-main-ci-parity-evidence-json-repair/`
Phase: `01.5 Root Cause Analysis`
Status: `LOCKED`
Addendum: `12`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run78-biome-debt.addendum-10.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run79.red.log`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run79-repro.red.log`
- User direction: fix the issues locally before pushing to GitHub
Outputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run79-biome-debt.addendum-12.md`
Scope note: This addendum records the next masked branch-CI blocker exposed after the run-78 remediation landed on the branch.

## TODO

- [x] Re-read the new run-79 branch-CI evidence and local reproduction log
- [x] Bound the newly exposed blocker to the actual run-79 file set
- [x] Distinguish this new layer from the earlier run-78 runtime-ui cleanup
- [x] Record the next fix direction for Phase 2
- [x] Complete the audited sections and gates

## Error Analysis

**New failing surface:** GitHub Actions run `#79` (`25702188627`) still fails in `validate -> CI parity`, but the run-78 blocker is gone. The newly exposed file set in `phase4-branch-ci-run79.red.log` and `phase4-branch-ci-run79-repro.red.log` is:

- `role-model-router/apps/runtime-ui/app/components/future-surface.tsx`
- `role-model-router/apps/runtime-ui/app/components/page-primitives.tsx`
- `role-model-router/apps/runtime-ui/app/routes/control-routing-strategy.tsx`
- `role-model-router/apps/runtime-ui/app/routes/control-runtime-config.tsx`
- `role-model-router/apps/runtime-ui/app/routes/dashboard.tsx`
- `role-model-router/vendor/llama-swap/ui-svelte/src/lib/histogram.test.ts`

**Observed failure summary:** the bounded run-79 evidence records:
- `format` failures in all six files
- `organizeImports` in `control-routing-strategy.tsx`, `control-runtime-config.tsx`, and `dashboard.tsx`
- explicit lint in repo-owned runtime-ui code:
  - `future-surface.tsx` `lint/style/useImportType`
  - `control-runtime-config.tsx` `lint/correctness/useExhaustiveDependencies`
- explicit lint in vendored Svelte test code:
  - `histogram.test.ts` repeated `lint/style/noNonNullAssertion`
- GitHub CI summary:
  - `Checked 459 files in 548ms. No fixes applied.`
  - `Found 286 errors.`

**Key insight:** the run-78 remediation worked. GitHub CI is now failing on a smaller mixed layer: five runtime-ui formatting/import files plus one vendored histogram test with repeated non-null assertions.

## Reproduction Verification

**Steps:**
1. Push commit `c0ce3bd`.
2. Wait for GitHub Actions run `25702188627`.
3. Inspect `phase4-branch-ci-run79.red.log`.
4. Re-run `corepack pnpm exec biome check` over the exact 6-file set listed above.
5. Confirm the local run fails on the same bounded file surface.

**Reproducible:** Yes
**Frequency:** 1 of 1 GitHub runs and 1 of 1 focused local reproductions
**Deterministic:** Yes

## Recent Changes Analysis

**Relevant change:** the run-78 remediation committed in `c0ce3bd` cleared the prior runtime-ui library/root/control-models layer.

**Likely culprit:** the run-78 fix was correctly bounded to its 10-file set. Because CI still executes `biome check .` across the whole repository, the next masked six-file layer surfaced immediately after the branch got past run `78`.

## Evidence Gathering

- Layer 1: GitHub Actions `CI parity`
  - Input: branch commit `c0ce3bd`
  - Output: 6-file runtime-ui-plus-vendor-test failure set with ordinary format/import diagnostics plus three explicit lint classes
  - Status: broken
- Layer 2: focused local repro
  - Input: the same 6 files from the GitHub log
  - Output: the local run fails on the same bounded file set
  - Status: broken
- Layer 3: blocker class comparison
  - Input: run `78` versus run `79` evidence
  - Output:
    - run `78`: runtime-ui library/root/controller/models formatter layer
    - run `79`: runtime-ui components/routes plus vendored histogram test assertions
  - Status: confirmed
LockedAt: `2026-05-11T22:58:56Z`
LockHash: `f1129d4ecb0022adcc0845ce3410d364613386409ddfae75a018b8239feb4f75`

**Failure boundary:** repo-wide `biome check .` -> next masked runtime-ui-plus-vendored-test formatter/lint layer

## Data Flow Trace

1. The run-78 remediation pushes branch CI past the earlier runtime-ui layer.
2. `pnpm run lint` still executes `biome check .` over the whole repository.
3. Biome now reaches a new bounded group of runtime-ui component/route files and one vendored histogram test and reports ordinary format / organize-import debt plus `useImportType`, `useExhaustiveDependencies`, and `noNonNullAssertion`.
4. The run exits with standard lint failure `1`, not a runtime behavior failure.

**Corrected root cause for the remaining CI failure:** run 31 now faces a seventh masked Biome layer: 6 files outside the earlier run-78 slice still need routine formatting/import cleanup and two small manual repairs in `control-runtime-config.tsx` and `histogram.test.ts`.

## Pattern Analysis

| File class | Example | Pattern |
| --- | --- | --- |
| Runtime-ui component/route scaffolding | `future-surface.tsx`, `dashboard.tsx` | ordinary formatting/import debt plus one import-type cleanup |
| Runtime-ui config route | `control-runtime-config.tsx` | formatter/import debt plus `useExhaustiveDependencies` around the initial load |
| Vendored Svelte histogram test | `vendor/llama-swap/ui-svelte/src/lib/histogram.test.ts` | repeated `noNonNullAssertion` sites in expectations after `expect(result).not.toBeNull()` |

## Hypothesis Testing

### Hypothesis 1

**Statement:** run `79` is no longer blocked by the run-78 file set.

**Rationale:** the new GitHub log contains a different six-file set and no references to the run-78 runtime-ui library/root/control-models files.

**Test:** inspect the run-79 GitHub CI log after pushing `c0ce3bd`.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run79.red.log`

### Hypothesis 2

**Statement:** the remaining blocker is bounded to a concrete six-file set.

**Rationale:** the GitHub log names a finite set of files, and the local focused repro fails on the same set.

**Test:** rerun Biome on only the run-79 files and inspect the failure surface.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run79-repro.red.log`

### Hypothesis 3

**Statement:** the new blocker is ordinary Biome debt plus two manual lint hotspots, not a deeper architectural issue.

**Rationale:** the run-79 evidence reports only formatter/import debt plus `useImportType`, `useExhaustiveDependencies`, and repeated `noNonNullAssertion` findings, then exits `1`.

**Test:** compare run-79 diagnostics with the earlier run-78 evidence and inspect the flagged `control-runtime-config.tsx` and `histogram.test.ts` sites.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-branch-ci-run79.red.log`

## Root Cause Summary

**Corrected Root Cause:** after the run-78 remediation removed the previous runtime-ui formatter layer, branch CI exposed a seventh masked Biome layer: 6 files still contain ordinary formatting/import debt plus one config-route effect dependency issue and repeated non-null assertions in a vendored histogram test.

**Location:** the 6 run-79 paths listed in `phase4-branch-ci-run79.red.log`, with explicit lint hotspots at `app/routes/control-runtime-config.tsx:60` and `vendor/llama-swap/ui-svelte/src/lib/histogram.test.ts`

**Detailed Explanation:** the branch keeps moving forward one masked lint layer at a time. The run-78 slice is no longer failing, which confirms that fix. The next remaining failure is again standard Biome debt, now split between repo-owned runtime-ui scaffolding and a vendored histogram test. No new product behavior is required. The required next step is a bounded cleanup of the run-79 file set only, followed by another local green proof before pushing.

**Fix Strategy:** add a focused regression covering the exact run-79 file set, apply Biome-managed formatting and organize-import cleanup to the six files, make the `control-runtime-config` load path dependency-safe, replace the vendored histogram test non-null assertions with guarded locals, rerun the focused local checks, then push and rerun branch CI.

**Test Plan:** preserve run `79` as RED evidence, capture a focused local RED Vitest regression over the 6-file set, capture focused local green Biome and Vitest runs over the same set, and rerun GitHub Actions CI only after the local bounded surface is green.

## Traceability

- `R4` -> this addendum explains why parity is still blocked after the run-78 remediation
- `R5` -> the run keeps widening only because GitHub CI continues to expose new confirmed blockers while the user has asked to continue fixing them locally first

## Effective-Input Rule For Later Phases

Until superseded by a later locked addendum, later phases for run 31 must treat this file as an authoritative effective input together with:

- `/.recursive/run/31-main-ci-parity-evidence-json-repair/01.5-root-cause.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-ci-run78-biome-debt.addendum-10.md`

This addendum adds the run-79 masked runtime-ui-plus-vendored-test Biome debt layer to the run.

## Coverage Gate

- [x] New failing surface identified
- [x] File scope bounded to the GitHub-reported and locally reproduced set
- [x] New blocker class distinguished from the earlier run-78 cleanup slice
- [x] New fix direction defined

Coverage: PASS

## Approval Gate

- [x] The remaining blocker is concrete and non-speculative
- [x] Scope expansion is grounded in continued explicit user direction
- [x] Later phases can plan the bounded repair without reopening earlier RCA

Approval: PASS

## Audit Verdict

- Audit summary: the remaining branch-CI blocker is now root-caused as a seventh masked Biome layer consisting of runtime-ui scaffolding plus a vendored histogram test across 6 files.
Audit: PASS
