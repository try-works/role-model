Run: `/.recursive/run/31-main-ci-parity-evidence-json-repair/`
Phase: `01.5 Root Cause Analysis`
Status: `LOCKED`
Addendum: `16`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/03-implementation-summary.branch-ci-run80-biome-debt.addendum-15.md`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-local-biome-after-run80.red.log`
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-local-biome-config-format-repro.red.log`
- User direction: continue fixing locally until ci passes
Outputs:
- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/01.5-root-cause.branch-local-biome-config-format.addendum-16.md`
Scope note: This addendum records the next blocker surfaced by local repo-wide Biome after the run-80 runtime-ui layer was cleared.

## TODO

- [x] Re-read the repo-wide local Biome log after the run-80 cleanup
- [x] Bound the newly exposed local blocker to a concrete file set
- [x] Confirm whether the new layer is format-only or requires manual lint fixes
- [x] Record the next Phase 2 direction
- [x] Complete the audited sections and gates

## Error Analysis

**New failing surface:** after the run-80 file set was cleared locally, running `corepack pnpm exec biome check .` surfaced a new 20-file batch in config and JSON assets. The repo-wide and focused local repro logs identify the same file set:

- `apps/docs-site/package.json`
- `apps/docs-site/tsconfig.json`
- `package.json`
- `packages/conformance/package.json`
- `packages/conformance/tsconfig.json`
- `packages/packaging/package.json`
- `packages/packaging/tsconfig.json`
- `packages/protocol-types/package.json`
- `packages/protocol-types/tsconfig.json`
- `packages/schema-tools/package.json`
- `packages/schema-tools/tsconfig.json`
- `packages/store-contract/package.json`
- `packages/store-contract/tsconfig.json`
- `role-model-router/apps/bench-cli/package.json`
- `role-model-router/apps/bench-cli/tsconfig.json`
- `role-model-router/apps/gateway-smoke/tsconfig.json`
- `role-model-router/apps/router-devtools/tsconfig.json`
- `role-model-router/apps/runtime-host-bridge/tsconfig.json`
- `testdata/traces/sample-trace.json`
- `tsconfig.base.json`

**Observed failure summary:** every surfaced diagnostic in this batch is `format`. No `lint/*`, `organizeImports`, or semantic code issues are present in the current bounded scope.

**Key insight:** the next local blocker is a pure formatter layer across workspace config files plus one JSON test fixture. That means the entire surfaced batch should be clearable with a bounded Biome write pass and a focused regression, without any behavioral code changes.

## Reproduction Verification

**Steps:**
1. Clear the run-80 runtime-ui file set locally.
2. Run repo-wide `corepack pnpm exec biome check .` and preserve the red output.
3. Extract the 20 surfaced file headings from the repo-wide log.
4. Re-run `corepack pnpm exec biome check` over only those 20 files.
5. Confirm the focused run fails on the same format-only surface.

**Reproducible:** Yes
**Frequency:** 1 of 1 repo-wide local runs and 1 of 1 focused local reproductions
**Deterministic:** Yes

## Recent Changes Analysis

**Relevant change:** the run-80 runtime-ui remediation is now locally green and locked.

**Likely culprit:** the repo-wide local Biome command progressed past the earlier route/helper slice and exposed the next remaining format-only debt in config and JSON files.

## Evidence Gathering

- Layer 1: repo-wide local Biome after run-80
  - Input: the branch state with the run-80 cleanup applied
  - Output: a 20-file format-only config/JSON blocker set
  - Status: broken
- Layer 2: focused local repro
  - Input: the same 20 config/JSON files
  - Output: the local run fails on the same bounded formatter surface
  - Status: broken
- Layer 3: diagnostic class comparison
  - Input: the current repo-wide and focused logs
  - Output: all surfaced diagnostics are `format` only
  - Status: confirmed
LockedAt: `2026-05-11T23:18:01Z`
LockHash: `e42986d526b704c885f537f16e3b9ae5e2ac7a015d67540db3b8f29a6e4b00e2`

**Failure boundary:** repo-wide local `biome check .` -> next masked config/JSON formatter layer

## Data Flow Trace

1. The run-80 cleanup removes the previous runtime-ui/vendored-helper blocker.
2. Repo-wide local Biome advances to the next remaining debt layer.
3. Biome reports only formatter deviations across workspace config files and one JSON fixture.
4. The run exits with formatter failure `1`, not a runtime or lint logic failure.

**Corrected root cause for the remaining local parity failure:** the next blocker after run-80 is a 20-file format-only layer in package manifests, tsconfig files, the workspace root config, and one JSON trace fixture.

## Pattern Analysis

| File class | Examples | Pattern |
| --- | --- | --- |
| Package manifests | `package.json`, `packages/*/package.json`, `apps/docs-site/package.json` | formatter-only debt |
| TypeScript configs | `tsconfig.base.json`, `packages/*/tsconfig.json`, `role-model-router/apps/*/tsconfig.json` | formatter-only debt |
| JSON fixture | `testdata/traces/sample-trace.json` | formatter-only debt |

## Hypothesis Testing

### Hypothesis 1

**Statement:** the next blocker after run-80 is local and does not require GitHub to expose it.

**Rationale:** the user explicitly asked for local-first iteration, and repo-wide local Biome now reproduces the next failure immediately.

**Test:** run `corepack pnpm exec biome check .` after clearing the run-80 set.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-local-biome-after-run80.red.log`

### Hypothesis 2

**Statement:** the blocker is bounded to the extracted 20-file config/JSON set.

**Rationale:** the focused repro over those exact paths fails on the same format-only surface.

**Test:** rerun Biome on only the extracted 20-file list.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-local-biome-config-format-repro.red.log`

### Hypothesis 3

**Statement:** the current local blocker requires only formatting, not manual lint or logic fixes.

**Rationale:** both logs show `format` diagnostics only.

**Test:** inspect the extracted file headings and reported diagnostic kinds in the two red logs.

**Result:** confirmed

**Evidence:** `/.recursive/run/31-main-ci-parity-evidence-json-repair/evidence/logs/red/phase4-local-biome-after-run80.red.log`

## Root Cause Summary

**Corrected Root Cause:** after the run-80 route/helper cleanup, local repo-wide Biome exposed the next masked debt layer: 20 config and JSON files that only need formatter alignment.

**Location:** the 20 files listed above, preserved in `phase4-local-biome-after-run80.red.log` and `phase4-local-biome-config-format-repro.red.log`

**Detailed Explanation:** this is a pure formatter slice. The branch is no longer failing on the earlier runtime-ui route/helper set. Local parity now stops at workspace config manifests, tsconfig files, the root config, and one JSON trace fixture. Because every surfaced diagnostic is `format`, the correct next step is a bounded write pass plus a regression that locks this exact file set into repeatable local proof.

**Fix Strategy:** add a focused regression covering the exact 20-file config/JSON set, run it red first, apply a bounded Biome write pass to those files, rerun the regression and the focused Biome check, then rerun repo-wide local Biome to see the next remaining layer.

**Test Plan:** preserve the repo-wide and focused red logs, add a focused Vitest regression over the same 20-file set, capture green evidence from the bounded regression and focused Biome check, then rerun repo-wide local Biome.

## Traceability

- `R4` -> local-first parity clearing continues with the next explicit blocker surfaced after run-80
- `R5` -> the scope remains strictly bounded to the extracted 20-file config/JSON set

## Effective-Input Rule For Later Phases

Until superseded by a later locked addendum, later phases for run 31 must treat this file as an authoritative effective input together with:

- `/.recursive/run/31-main-ci-parity-evidence-json-repair/addenda/03-implementation-summary.branch-ci-run80-biome-debt.addendum-15.md`

This addendum adds the local config/JSON formatter batch that surfaced after run-80.

## Coverage Gate

- [x] New failing surface identified
- [x] File scope bounded to the repo-wide and focused local reproduction set
- [x] Diagnostic class confirmed as format-only
- [x] New fix direction defined

Coverage: PASS

## Approval Gate

- [x] The remaining blocker is concrete and non-speculative
- [x] Scope expansion is grounded in continued explicit user direction
- [x] Later phases can plan the bounded repair without reopening earlier RCA

Approval: PASS

## Audit Verdict

- Audit summary: the remaining local parity blocker is now root-caused as a 20-file config/JSON formatter layer exposed immediately after the run-80 cleanup.
Audit: PASS
