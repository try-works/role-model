{
  "ProcessId": 64568,
  "ParentProcessId": 48772,
  "Name": "pwsh.exe",
  "CommandLine": "\"C:\\Program Files\\PowerShell\\7\\pwsh.exe\" -Command \"try { [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}\n$ErrorActionPreference = 'Stop'; $repo = 'D:\\DEV\\role-model\\.worktrees\\62-litellm-pi-craft-codex-execution-hardening'; $liveDir = Join-Path $repo '.recursive\\run\\62-litellm-pi-craft-codex-execution-hardening\\evidence\\logs\\addendum-16\\live\\craft-headless'; $tmpRoot = Join-Path $repo '.tmp-craft-headless-addendum16'; $userData = Join-Path $tmpRoot 'user-data'; $configDir = Join-Path $tmpRoot 'config'; New-Item -ItemType Directory -Force -Path $liveDir,$userData,$configDir | Out-Null; $existing = @(Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match '\\.tmp-craft-headless-addendum16' } | Select-Object ProcessId,ParentProcessId,Name,CommandLine); $existing | ConvertTo-Json -Depth 6 | Set-Content -Encoding utf8 (Join-Path $liveDir 'craft-headless-processes-before-start.json'); foreach ($proc in $existing) { Stop-Process -Id $proc.ProcessId -Force -ErrorAction SilentlyContinue }; $exe = 'C:\\Users\\erikb\\AppData\\Local\\Programs\\@craft-agentelectron\\Craft Agents.exe'; $stdout = Join-Path $liveDir 'craft-headless.stdout.log'; $stderr = Join-Path $liveDir 'craft-headless.stderr.log'; Remove-Item -LiteralPath $stdout,$stderr -Force -ErrorAction SilentlyContinue; $env:CRAFT_HEADLESS = '1'; $env:CRAFT_CONFIG_DIR = $configDir; $env:CRAFT_RPC_HOST = '127.0.0.1'; $env:CRAFT_RPC_PORT = '0'; $proc = Start-Process -FilePath $exe -ArgumentList @('--user-data-dir', $userData) -WindowStyle Hidden -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru; [pscustomobject]@{ pid=$proc.Id; userData=$userData; configDir=$configDir; stdout=$stdout; stderr=$stderr; startedAt=(Get-Date).ToString('o') } | ConvertTo-Json -Depth 6 | Set-Content -Encoding utf8 (Join-Path $liveDir 'craft-headless-start.json'); $url=$null; $token=$null; $deadline=(Get-Date).AddSeconds(60); while ((Get-Date) -lt $deadline) { if (Test-Path $stdout) { $content=Get-Content -Raw $stdout -ErrorAction SilentlyContinue; if ($content -match 'CRAFT_SERVER_URL=(\\S+)') { $url=$Matches[1] }; if ($content -match 'CRAFT_SERVER_TOKEN=(\\S+)') { $token=$Matches[1] }; if ($url -and $token) { break } }; Start-Sleep -Milliseconds 500 }; $result=[pscustomobject]@{ pid=$proc.Id; hasExited=$proc.HasExited; url=$url; tokenPresent=[bool]$token; stdout=$stdout; stderr=$stderr }; $result | ConvertTo-Json -Depth 6 | Set-Content -Encoding utf8 (Join-Path $liveDir 'craft-server.json'); if (-not $url -or -not $token) { Get-Content $stderr -ErrorAction SilentlyContinue | Select-Object -Last 80; throw 'Craft headless server did not emit CRAFT_SERVER_URL/CRAFT_SERVER_TOKEN' }; $result | ConvertTo-Json -Depth 6\""
}
