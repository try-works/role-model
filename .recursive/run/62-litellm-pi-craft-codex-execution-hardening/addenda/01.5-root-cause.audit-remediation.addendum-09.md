Run: `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/`
Phase: `01.5 Root Cause Analysis`
Addendum: `09`
Status: `LOCKED`
LockedAt: `2026-07-10T04:28:10Z`
LockHash: `e7bd358a71b01d98772750e8fc89ca7b64c171a2c9706f61ce6385a8629a43b2`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/evidence/logs/final-ci/ci-check.failed-vendor-timeout.log`
- `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/evidence/logs/final-ci/ci-check.log`
Outputs:
- `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/addenda/01.5-root-cause.audit-remediation.addendum-09.md`
Scope note: This addendum records the final local-CI remediation found while preparing run 62 for commit after addendum 08 was already locked.

## TODO

- [x] Analyze the final local-CI failure.
- [x] Reproduce and classify the CI harness failure mode.
- [x] Define the remediation strategy without weakening the Pi/Craft validator corpus.

## Error Analysis

The full `corepack pnpm run ci:check` run failed in `@role-model-router/runtime-host-bridge`:

- `test/validate-vendors.test.ts` timed out at `90000ms`
- the same canceled test then reported `ENOTEMPTY` while removing `runtime-vendor-validation-hybrid/memory`
- the failing test was the intentionally large end-to-end vendor validator covering decision-only, local-only, remote-only, hybrid, Pi Responses, and Craft Chat routes

Earlier local-CI attempts also surfaced smaller harness issues before this final blocker:

- Pi validate-agent-path test used a dynamic import shape that Vitest could not execute in transformed context.
- Schema fixture-manifest validation exceeded the default timeout under full workspace load.
- Runtime-host-bridge integration suites exceeded Vitest's default timeout under full workspace load.
- One controller-hybrid corpus case was over-specified to a single execution family even though the alias/controller contract permits either local or LiteLLM execution.

## Reproduction Verification

- Full CI failure: `corepack pnpm run ci:check`
- Isolated vendor validator before the final timeout change completed near the old ceiling, proving the validator itself was valid but not robust to full workspace load.
- Final full CI after remediation passed with `validate-vendors.test.ts` completing in `108856ms`.

Evidence:

- RED: `evidence/logs/final-ci/ci-check.failed-vendor-timeout.log`
- GREEN: `evidence/logs/final-ci/ci-check.log`

## Pattern Analysis

Working examples in the same package already use longer budgets for integration-style work, while the vendor validator remained constrained by an explicit `90_000ms` timeout despite now running a 200-case deterministic Pi/Craft corpus. The cleanup failure occurred after timeout cancellation on Windows, which can leave SQLite/runtime memory files visible briefly after runtime shutdown.

## Root Cause Summary

Root cause: final local CI was exercising a large integration validator under full workspace parallelism with a timeout budget below observed full-load runtime, and timeout cancellation exposed a Windows temp-directory deletion race.

Fix strategy:

- Keep the comprehensive validator and corpus intact.
- Raise only the oversized vendor validator's explicit per-test budget.
- Make recursive temp-root deletion retry-tolerant.
- Preserve the failure log and prove the fix through a full green `ci:check`.

## Coverage Gate

- [x] Error messages analyzed
- [x] Reproduction path identified
- [x] Working/failed pattern compared
- [x] Root cause documented
- [x] Fix strategy defined without weakening coverage

Coverage: PASS

## Approval Gate

- [x] Root cause is CI harness budget and cleanup robustness, not routing semantics
- [x] Plan preserves Pi/Craft alias corpus coverage
- [x] Full local CI is required as the acceptance gate

Approval: PASS
