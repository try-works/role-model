Run: `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/`
Phase: `03.5 Code Review`
Addendum: `16`
Status: `LOCKED`
LockedAt: `2026-07-09T14:05:55Z`
LockHash: `a73c34e71be8b33e0313eff0a66ce032fd87aa30c93deaea050e4697aace73fc`
Workflow version: `recursive-mode-audit-v1`
Inputs:
- `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/addenda/03-implementation-summary.provider-agnostic-routing-preferences.addendum-16.md`
- `/role-model-router/apps/runtime-host-bridge/src/index.ts`
- `/role-model-router/apps/runtime-host-bridge/test/index.test.ts`
- `/role-model-router/apps/runtime-host-bridge/src/validate-vendors.ts`
- `/role-model-router/apps/runtime-host-bridge/test/validate-vendors.test.ts`
- `/packages/pi-role-model/test/validate-agent-path.test.ts`
- `/scripts/validate-agent-path.ts`
Outputs:
- `/.recursive/run/62-litellm-pi-craft-codex-execution-hardening/addenda/03.5-code-review.provider-agnostic-routing-preferences.addendum-16.md`
Scope note: Review covers addendum 16 provider-agnostic routing preference changes only. It does not re-review unrelated run-62 addenda, generated evidence, or pre-existing worktree changes.

# Addendum 16 Code Review

## TODO

- [x] Review for remaining provider-specific routing preference or hard pinning.
- [x] Review for accidental removal of provider-specific execution adapter selection.
- [x] Review tests for specificity and regression coverage.
- [x] Review validation helpers for stale hard-winner assumptions.
- [x] Review docs and durable memory for conflicting routing guidance.
- [x] Record residual risks.

## Findings

No open blocking findings remain.

Resolved during implementation review:

- Stale validator and Pi helper expectations still assumed Codex would win hard/tool alias cases. These were corrected so tests assert candidate eligibility and selected-endpoint identity instead of a fixed provider winner.
- `scripts/validate-agent-path.ts` did not expose eligible endpoint IDs in summaries, which made addendum verification weaker than required. The summary now includes `eligibleEndpointIds`.

## Review Notes

Runtime routing source:

- The removed helpers are absent from `/role-model-router/apps/runtime-host-bridge/src/index.ts`: `applyOpenAICodexSubscriptionInitialPin`, `resolveOpenAICodexSubscriptionRoutingModel`, `shouldPreferOpenAICodexSubscriptionForTurn`, `preferredCodexRoutingModel`, and `fallbackAllowEndpoints`.
- Remaining Codex-specific endpoint detection is limited to adapter/execution identity after endpoint selection, which is inside the allowed provider-execution boundary.
- `routeExecutionRequest()` no longer swaps to a Codex-specific fallback allow-list. It applies denied endpoint IDs to the provider-agnostic routing request.

Test coverage:

- `runtime-host-bridge/test/index.test.ts` has a direct provider-agnostic alias test and a source guard for removed pin helpers.
- `alias-capability-routing.test.ts` remains the guard for metadata-based narrowing.
- `core/test/routing-intent.test.ts` remains the guard for advisory scoring rather than hard filtering.
- `validate-vendors.test.ts` and `packages/pi-role-model/test/validate-agent-path.test.ts` were updated to verify selected endpoint identity without assuming a provider-specific winner.

Docs and memory:

- Architecture docs, `STATE`, `DECISIONS`, and routing durable memory now supersede the old Codex-first guidance.
- The corrected guidance distinguishes provider identity from vendor/adapter/execution path.

## Residual Risks

- Full-run `verify-locks` still fails because unrelated earlier addenda in the same worktree remain draft or malformed. Addendum 16 artifacts are valid and locked separately.
- Live routing winners depend on benchmark and measured scoring. The addendum verifies eligibility and scoring, not a deterministic winner.
- Some old telemetry rows from prior binaries may remain in the user state root. Manual QA uses request IDs created after the rebuilt runtime launch.

## Subagent Contribution Verification

- Reviewed Action Records: none.
- Main-Agent Verification Performed: source diff review, source-string guard review, test expectation review, CI result review, and live router decision review.
- Acceptance Decision: self-audit accepted.
- Refresh Handling: no delegated action records were created.
- Repair Performed After Verification: stale validation expectations and summary evidence gaps were fixed before final CI and runtime verification.

## Coverage Gate

- [x] Provider-specific routing pin absence reviewed.
- [x] Provider-specific execution adapter boundary reviewed.
- [x] Tests reviewed for provider-agnostic assertions.
- [x] Docs and memory reviewed for conflicting guidance.
- [x] Residual risks recorded.

Coverage: PASS

## Approval Gate

- [x] No open code-review findings remain.
- [x] The implementation is ready for Phase 4 and Phase 5 evidence.

Approval: PASS
