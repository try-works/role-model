# Detect Endpoints Skill

This entry point covers ACP, MCP, and CLI endpoint discovery from declared metadata.
The baseline normalizes these candidates into protocol-shaped endpoint identity and capability records.
