# Benchmark Skill

This entry point describes endpoint-centric benchmark execution for the baseline fixture suites.
It is intentionally small, but it preserves the package and contract surface needed for later growth.
