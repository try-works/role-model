# Native Router Workspace

This workspace reserves native implementation homes for future router hosts, provider bridges, and stores.
The stable baseline uses Rust stable plus a Cargo workspace, but the crates remain intentionally skeletal.
