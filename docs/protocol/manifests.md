# Manifests

Two manifest families are reserved in the baseline:

- `PackageManifest` for reusable package/distribution metadata,
- `ModelPackManifest` for future model-pack and bundle metadata.

Both are documented now so packaging work later extends a stable protocol surface rather than creating a
parallel manifest language.
