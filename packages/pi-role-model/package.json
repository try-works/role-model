{
  "name": "pi-role-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pi package for connecting Pi to an externally running Role-Model runtime.",
  "files": ["extensions", "skills", "src"],
  "pi": {
    "extensions": ["extensions/role-model.ts"],
    "skills": ["skills"]
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
